# «پنل من» — راهنمای نصب

## ماجرا چی بود؟
۴ فایلی که قبلاً برات ساخته شده بود (`index.html`, `hub.js`, `hub.css`,
`clock.js`) فقط ظاهر مینی‌اپ بودن — به هیچ بک‌اندی وصل نبودن. من این‌ها رو
کامل کردم: دیتابیس، API، دکمه‌ی کنار چت، همه رو اضافه کردم تا واقعاً کار کنه.

## فایل‌ها کجا می‌رن؟

جایگزین کن (این ۳ تا از قبل تو پروژه‌ت هستن، نسخه‌ی جدیدشون رو جایگزین کن):
- `database.py`
- `bot.py`
- `game_server.py`

جدید اضافه کن:
- `hub.py` → کنار بقیه‌ی فایل‌های پایتون، ریشه‌ی پروژه
- پوشه‌ی `hub/` (شامل `index.html`, `hub.js`, `hub.css`, `clock.js`) → کنار
  پوشه‌ی `webapp/` موجودت، همون سطح

## دقیقاً چی عوض شده؟

### `database.py`
- ۴ ستونِ جدید به جدول `admins` اضافه شد: `title`, `city`, `bio`, `details`
  (با ALTER TABLE امن، دیتای موجودت دست نمی‌خوره)
- چند تابع جدید فقط برای هاب اضافه شد (همه‌شون با کامنت مشخصن، دنبال
  `# ─── «پنل من» (hub.py)` بگرد)

### `bot.py`
- بعد از تنظیم دستورات ربات، دکمه‌ی «پنل من» کنار چت برای پیشوا و مدیرهای
  فعال ست می‌شه (اگه `WEBAPP_URL` تنظیم نشده باشه، این کار رو انجام نمی‌ده،
  نه خطا می‌ده)
- یه job هر ۱ ساعت اجرا می‌شه که این دکمه رو برای مدیرهای تازه هم به‌روز
  می‌کنه (چون create_admin تو ۵ جای مختلف صدا زده می‌شه و قلاب‌زدن به همه‌شون
  ریسکِ خراب‌کردنِ چیزی که کار می‌کنه رو داشت)

### `game_server.py`
- یه خط اضافه شد که `hub.py` رو روی همون سروری که `admin_panel`/
  `principal_panel` روشن، سوار می‌کنه (همون الگو، سرور جدا نیست)

### `hub.py` (جدید)
بک‌اند کامل هاب:
- احراز هویت با initData امضاشده‌ی تلگرام (دقیقاً مثل مینی‌اپ شطرنج زنده‌ت
  — نه رمز، نه کلید تو URL)
- مسیرها: `/hub/api/bootstrap`, `/hub/api/players`, `/hub/api/player/{id}`,
  `/hub/api/tournament/{id}`, `/hub/api/profile` (تنها مسیر نوشتنی)
- پیشوا (PISHVA_ID) تو جدول admins ردیف نداره؛ پروفایلش جدا تو
  system_settings نگه داشته می‌شه — قبل از هر تغییری تو این بخش، این نکته
  رو یادت باشه

### `hub/index.html` و `hub/hub.js`
- `logo.webp` که وجود نداشت رو با یه crest به‌شکل SVG (مهره‌ی شطرنج، هم‌رنگ
  تم آبی) جایگزین کردم — چون تو کل پروژه از قبل همه‌ی آیکون‌ها SVG بودن، نه
  فایل تصویر جدا

## قبل از دیپلوی چک کن
1. `WEBAPP_URL` تو تنظیمات محیطی‌ت (env vars روی Railway) درست و HTTPS باشه —
   بدونش دکمه‌ی «پنل من» اصلاً ست نمی‌شه.
2. بعد از اولین اجرا با کد جدید، `init_db()` خودکار صدا زده می‌شه و ستون‌های
   جدید admins اضافه می‌شن — نیازی به کار دستی نیست.
3. یه بار ربات رو ری‌استارت کن تا دکمه‌ی «پنل من» برای پیشوا و مدیرهای فعال
   ست بشه (یا صبر کن، تا ۱۰ دقیقه بعد از استارت خودش اجرا می‌شه).
4. از تلگرام (نه مرورگر معمولی) روی دکمه‌ی «پنل من» بزن و امتحان کن.

## اگه چیزی کار نکرد
- کنسول/لاگ سرور رو چک کن؛ هر مسیر با کد ۴۰۱ (خارج از تلگرام باز شده) یا
  ۴۰۳ (تو ادمین فعال نیستی) جواب میده، نه خطای خام.
- اگه دکمه‌ی کنار چت نیومد: `WEBAPP_URL` رو چک کن، بعد لاگِ
  `Hub menu button synced for X chats` رو تو لاگ سرور پیدا کن.
