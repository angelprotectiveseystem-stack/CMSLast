# راهنمای جامع دستیار هوشمند — سیستم فرماندهی شطرنج (CMSLast)

این فایل، دانشِ کاملِ ربات را یک‌جا در اختیار دستیار هوشمند (ai_assistant.py) می‌گذارد:
مسیر دقیق تمام دکمه‌ها، تمام دستورات کلمه‌ای (keyword)، تمام ابزارهایی که خودِ دستیار
مستقیم اجرا می‌کند، و راهنمای گام‌به‌گام هر بخش — همه به‌تفکیکِ نقش.

## ۱) نقش‌ها و مدل دسترسی

سیستم فقط **سه نقشِ ادمین** دارد (بازیکن‌ها کاربر ربات نیستند، فقط در دیتابیس مدیریت می‌شوند):

| نقش | کد داخلی | سطح دسترسی |
|---|---|---|
| 👑 مدیر ارشد (پیشوا) | `pishva` | بالاترین سطح — یک نفر ثابت (`PISHVA_ID`)، به همه‌چیز دسترسی دارد |
| 🏆 مدیر مسابقات | `tournament_manager` | مدیریت بازیکنان و مسابقات، بدون دسترسی به بخش‌های امنیتی/تنظیمات/مدیریت مدیران |
| 🛡️ مدیر امنیتی | `security_manager` | مدیریت بازیکنان (محدودتر) + صف انتظار عضویت و بلاک‌کردن کاربران |

نکات مهم برای دستیار:
- «مدیر ارشد» با آی‌دی تلگرام ثابت (`PISHVA_ID`) شناسایی می‌شود، نه با نقش در دیتابیس.
- هر مدیر معمولی (تورنمنت/امنیتی) می‌تواند مجموعه‌ای از **دسترسی‌های تفصیلی** هم داشته باشد
  (مثلاً `senior_admin`, `direct_ban`, `edit_delete_match`, `ai_access` و…) که مدیر ارشد از
  «مدیریت مدیران ← عملیات روی مدیر ← دسترسی‌ها» روشن/خاموش می‌کند. این‌ها مستقل از سقفِ نقش‌اند.
- هر ابزارِ دستیار، هم به سقفِ نقش (جدول بخش ۴) محدود است، هم به این‌که مدیر ارشد آن دسته‌ابزار را
  برای دستیار فعال نگه داشته باشد (پنل «🧑‍💻 مدیریت دستیار ← 🛠️ اختیارات دستیار»).

---

## ۲) نقشه‌ی کامل منوها و دکمه‌ها (به ترتیبِ دقیقِ نمایش)

قواعد خواندن این بخش:
- هر بلوک یک تابعِ کیبورد (`kb_...`) را نشان می‌دهد؛ نامِ فارسیِ منو جلوی آن آمده.
- دکمه‌ها دقیقاً به همان **ترتیب و متنی** که در ربات دیده می‌شوند فهرست شده‌اند.
- `<...>` یعنی مقدار پویاست (مثلاً نامِ بازیکن)؛ `*` در callback یعنی شناسه‌ی پویا (آی‌دی).
- «→ back_x» یعنی این دکمه کاربر را به منوی x برمی‌گرداند.


── منوهای اصلی (بر اساس نقش) ──

**منوی اصلی — مدیر ارشد**  `(kb_pishva_main)`
- «♟️ مدیریت مسابقات» → `menu_matches`
- «👤 مدیریت بازیکنان» → `menu_players`
- «👑 پنل مدیر ارشد» → `menu_pishva`
- «👥 مدیریت مدیران» → `menu_admins`
- «📡 مخابرات» → `menu_comms`
- «📋 وظایف» → `menu_tasks`
- «📊 داشبورد مدیر ارشد» → `dashboard_pishva`
- «❓ راهنما» → `menu_help`
- «🗄️ وضعیت دیتابیس» → `pishva_dbstatus`
- «♟️ شطرنج زنده» → `chess_menu`
- «🤖 دستیار هوشمند» → `ai_assistant_open`
- «🧑‍💻 مدیریت دستیار» → `ai_manage_menu_main`
- «💡 انتقادات و پیشنهادات» → `menu_feedback`

**منوی اصلی — مدیر مسابقات**  `(kb_tournament_manager_main)`
- «♟️ مدیریت مسابقات» → `menu_matches`
- «👤 مدیریت بازیکنان» → `menu_players`
- «📊 داشبورد من» → `dashboard_admin`
- «📡 مخابرات» → `menu_comms`
- «📋 وظایف» → `menu_tasks`
- «❓ راهنما» → `menu_help`
- «♟️ شطرنج زنده» → `chess_menu`
- «🤖 دستیار هوشمند» → `ai_assistant_open`
- «💡 انتقادات و پیشنهادات» → `menu_feedback`

**منوی اصلی — مدیر امنیتی**  `(kb_security_manager_main)`
- «👤 مدیریت بازیکنان» → `menu_players`
- «📊 داشبورد من» → `dashboard_admin`
- «📡 مخابرات» → `menu_comms`
- «📋 وظایف» → `menu_tasks`
- «❓ راهنما» → `menu_help`
- «💡 انتقادات و پیشنهادات» → `menu_feedback`
- «♟️ شطرنج زنده» → `chess_menu`
- «🤖 دستیار هوشمند» → `ai_assistant_open`


── مسابقات ──

**منوی مدیریت مسابقات**  `(kb_matches_menu)`
- «➕ ثبت مسابقه جدید» → `match_add`
- «🏆 ثبت نتیجه» → `match_result`
- «🔍 تاریخچه مسابقات» → `match_history`
- «📊 پنل مدیریت» → `match_panel`
- «🎲 قرعه‌کشی» → `lottery_start`
- «🎯 قرعه‌کشی پیشرفته» → `adv_lottery_start`
- «📋 جدول مسابقات» → `match_bracket`
- «🏅 تورنمنت‌ها» → `menu_tournament`
- «🏆 تیم‌ها» → `teams_menu`
- «📊 جدول Elo» → `elo_leaderboard`
- «👑 قهرمانان» → `champions`
- «🔙 بازگشت» → `back_main`

**گزینه‌های ثبت نتیجه (بعد از انتخاب مسابقه)**  `(kb_match_result_options)`
- «🥇 برد <نام>» → `result_white_*`
- «🥇 برد <نام>» → `result_black_*`
- «🤝 تساوی» → `result_draw_*`
- «❌ لغو مسابقه» → `result_cancel_*`
- «🔙 بازگشت» → `match_result`

**علت تساوی**  `(kb_draw_reasons)`
- «🔒 پات» → `draw_pat_*`
- «⏱️ اتمام زمان» → `draw_time_*`
- «♾️ حرکات بسیار» → `draw_moves_*`
- «🔁 سه تکرار» → `draw_repeat_*`
- «📝 سایر موارد» → `draw_other_*`
- «🔙 بازگشت» → `match_result`

**سوال حذف بازنده؟**  `(kb_eliminate_ask)`
- «✅ بله، <نام> حذف شود» → `eliminate_yes_*`
- «❌ خیر، ادامه دهد» → `eliminate_no_*`

**فیلتر تاریخچه مسابقات**  `(kb_match_history_filter)`
- «📅 امروز» → `mhist_today`
- «📆 این هفته» → `mhist_week`
- «🗓️ این ماه» → `mhist_month`
- «📚 کل مسابقات» → `mhist_all`
- «🔍 جستجو» → `mhist_search`
- «🔙 بازگشت» → `back_matches`

**عملیات روی یک مسابقه (در تاریخچه)**  `(kb_match_item_actions)`
- «✏️ ویرایش» → `match_edit_*`
- «🗑️ حذف» → `match_delete_*`
- «📌 پین کردن» → `match_pin_*`
- «🔙 بازگشت» → `match_history`

**منوی تورنمنت‌ها (زیرمنوی مسابقات)**  `(kb_tournament_menu)`
- «➕ افزودن تورنمنت» → `tourn_add`
- «⚙️ مدیریت تورنمنت» → `tourn_manage`
- «📌 تورنمنت پیش‌فرض» → `tourn_default`
- «📊 جزئیات فعال» → `tourn_details`
- «🗂️ حذف‌شده‌ها» → `tourn_deleted`
- «🔙 بازگشت» → `back_matches`

**عملیات روی یک تورنمنت**  `(kb_tournament_actions)`
- «✏️ ویرایش نام» → `tourn_edit_*`
- «🔴 پایان تورنمنت» → `tourn_end_*`
- «⏸️ به تعویق» → `tourn_pause_*`
- «📌 تنظیم پیش‌فرض» → `tourn_setdefault_*`
- «🗑️ حذف تورنمنت» → `tourn_delete_*`

**لیست تورنمنت‌ها**  `(kb_tournament_list)`
- «<...> <نام>» → `tourn_select_*`

**قرعه‌کشی ساده — محدوده**  `(kb_lottery_scope)`
- «🏫 فقط از یک کلاس» → `lottery_class`
- «🌐 از همه کلاس‌ها» → `lottery_all`
- «🔙 بازگشت» → `back_matches`

**قرعه‌کشی پیشرفته — محدوده**  `(kb_adv_lottery_scope)`
- «🎯 هر دو طرف از یک کلاس» → `adv_scope_same`
- «🔀 هرکدام از یک کلاس متفاوت» → `adv_scope_diff`
- «🌐 از همه کلاس‌ها (آزاد)» → `adv_scope_open`
- «🔙 بازگشت» → `back_matches`


── تیم‌ها ──

**منوی تیم‌ها**  `(kb_teams_menu)`
- «📋 تیم‌ها» → `teams_list`
- «➕ افزودن تیم» → `teams_add`
- «⚙️ تنظیمات تیم» → `teams_settings`
- «🔙 بازگشت» → `back_matches`

**لیست تیم‌ها**  `(kb_team_list)`
- «🏆 <نام>» → `team_view_*`

**عملیات روی یک تیم**  `(kb_team_actions)`
- «👥 بازیکنان تیم» → `team_members_*`
- «👑 تنظیم سرگروه» → `team_captain_*`
- «⚠️ اخطارهای تیم» → `team_warnings_*`
- «🗑️ حذف تیم» → `team_delete_*`
- «🔙 بازگشت» → `teams_list`


── بازیکنان ──

**منوی مدیریت بازیکنان**  `(kb_players_menu)`
- «➕ ثبت‌نام بازیکن» → `player_add`
- «🏫 مدیریت کلاس‌ها» → `class_manage`
- «📋 ثبت‌نام گروهی» → `bulk_register_start`
- «👁️ مشاهده بازیکنان» → `player_list`
- «🔍 جستجو بازیکن» → `player_search`
- «✅ ادامه‌دهندگان» → `player_continuing`
- «❌ حذف‌شدگان» → `player_eliminated`
- «🌟 بازیکنان برتر» → `player_elite`
- «⚡ نیروهای ویژه» → `player_special`
- «🔙 بازگشت» → `back_main`

**مدیریت کلاس‌ها**  `(kb_class_manage)`
- «✅ ثبت کلاس جدید» → `class_add`
- «👁️ مشاهده کلاس‌ها» → `class_list`
- «🔙 بازگشت» → `back_players`

**لیست کلاس‌ها**  `(kb_class_list)`
- «🏫 <نام>» → `class_select_*`

**عملیات روی یک کلاس**  `(kb_class_actions)`
- «📈 عملکرد کلاس» → `class_perf_*`
- «🗑 حذف کلاس» → `class_harddelete_ask_*`
- «👥 بازیکنان کلاس» → `class_players_*`
- «✏️ ویرایش نام» → `class_edit_*`
- «🔙 بازگشت» → `class_list`

**لیست بازیکنان (صفحه‌بندی‌شده)**  `(kb_player_list)`
- «<نام>» → `player_view_*`
- «◀️ قبلی» → `player_list_page_*_*`
- «▶️ بعدی» → `player_list_page_*_*`
- «🔍 جستجو بازیکن» → `player_search_ctx_*`

**انتخاب بازیکن (برای مسابقه و...)**  `(kb_player_select)`
- «<...> <نام> [<نام>]» → `*_*`
- «◀️ قبلی» → `*page_*`
- «بعدی ▶️» → `*page_*`

**عملیات روی یک بازیکن**  `(kb_player_actions)`
- «✏️ ویرایش نام» → `player_editname_*`
- «🏫 ویرایش کلاس» → `player_editclass_*`
- «⚠️ ثبت اخطار» → `player_warn_*`
- «🚫 اخراج» → `player_kick_*`
- «⏸️ تعلیق» → `player_suspend_*`
- «🔄 احیا» → `player_revive_*`
- «📝 یادداشت» → `player_note_*`
- «🌟 ثبت برتر» → `player_elite_*`
- «📈 امتیاز Elo» → `elo_player_*`
- «🔮 پیش‌بینی» → `predict_select_*`
- «⚡ ثبت ویژه» → `player_special_*`
- «🗑 حذف کامل» → `player_harddelete_ask_*`


── پنل مدیر ارشد ──

**پنل مدیر ارشد (👑 پنل مدیر ارشد)**  `(kb_pishva_panel)`
- «🚦 مدیریت وضعیت» → `pishva_status`
- «⚙️ تنظیمات ربات» → `pishva_settings`
- «🔍 پیگیری اقدامات» → `pishva_logs`
- «📥 درخواست‌های دسترسی» → `pishva_requests`
- «♟️ بازی‌های مدیران» → `pishva_chess_games`
- «💾 دریافت بکاپ» → `pishva_backup`
- «🕐 ساعت کاری» → `pishva_workhours`
- «🔧 حالت تعمیر» → `pishva_repair`
- «🏦 خزانه مدیر ارشد» → `pishva_vault`
- «🪪 تغییر هویت» → `pishva_identity`
- «🎓 سال تحصیلی جدید» → `pishva_newyear`
- «🔄 آپدیت ربات» → `pishva_update`
- «📡 گروه اعلانات» → `pishva_group`
- «🆔 تنظیم کانال اعلانات» → `pishva_channel`
- «📡 پخش خودکار» → `pishva_broadcast`
- «🛡️ پنل امنیتی APS» → `security_panel`
- «🧑‍💻 مدیریت دستیار» → `ai_manage_menu`
- «⏰ یادآورها» → `pishva_reminders`
- «🤖 کارهای دستیار» → `pishva_ai_scheduled`
- «🔙 بازگشت» → `back_main`

**انتخاب وضعیت سیستم**  `(kb_status_select)`
- «🟢 نرمال» → `set_status_normal`
- «🟡 بد» → `set_status_bad`
- «🔴 خطرناک» → `set_status_danger`
- «🪽 APS» → `set_status_aps`
- «🔙 بازگشت» → `menu_pishva`

**تنظیمات ربات (روشن/خاموش‌کن‌ها)**  `(kb_pishva_settings_simple)`
- «🔔 اعلانات» → `setting_notifications`
- «📡 مخابرات» → `setting_communications`
- «❓ راهنما» → `setting_help`
- «♟️ ثبت مسابقه» → `setting_match_reg`
- «🚪 ورود ادمین» → `setting_admin_login`
- «💤 خاموش برای ادمین‌ها» → `setting_bot_active`
- «🏆 حالت تیمی» → `setting_team_mode`
- «📝 ثبت‌نام با تیم» → `setting_team_reg`
- «👤 مدیران سازنده تیم» → `setting_mgr_team`
- «📊 داشبورد ادمین‌ها» → `setting_admin_dashboard`
- «🤖 هوش مصنوعی» → `setting_ai_online`
- «♟️ شطرنج زنده» → `setting_live_chess`
- «🚨 گزارش باگ به مدیر ارشد» → `setting_bug_report`
- «🏫 پنل وب مدیر مدرسه» → `setting_principal_panel`
- «🌐 پنل وب ادمین‌ها» → `setting_admin_webpanel`
- «🚨 هشدار حذف مشکوک ⚙️» → `sadel_panel`
- «🔙 بازگشت» → `menu_pishva`

**منوی بکاپ**  `(kb_backup_main)`
- «📅 امروز» → `backup_period_today`
- «📆 این هفته» → `backup_period_week`
- «🗓️ این ماه» → `backup_period_month`
- «📚 از ابتدا» → `backup_period_all`
- «🔄 تنظیمات بکاپ خودکار» → `pishva_auto_backup`
- «📥 بازگردانی از فایل» → `pishva_restore`
- «🔙 بازگشت» → `menu_pishva`

**بازه‌ی بکاپ**  `(kb_backup_period)`
- «📅 امروز» → `backup_period_today`
- «📆 این هفته» → `backup_period_week`
- «🗓️ این ماه» → `backup_period_month`
- «📚 از ابتدا» → `backup_period_all`
- «🔙 بازگشت» → `menu_pishva`

**فرمت بکاپ**  `(kb_backup_format)`
- «📄 Word» → `backup_fmt_word`
- «📊 Excel» → `backup_fmt_excel`
- «🔙 بازگشت» → `pishva_backup`

**تایید بازگردانی بکاپ**  `(kb_restore_confirm)`
- «✅ تایید و اعمال» → `restore_apply`
- «❌ انصراف» → `restore_cancel`

**منوی ساعت کاری**  `(kb_workhours)`
- «🟢 آغاز ساعت کاری» → `wh_start`
- «🔴 پایان ساعت کاری» → `wh_end`
- «⏱ پایان خودکار: <...>» → `wh_autoend_toggle`
- «⏰ یادآور عدم پایان: <...>» → `wh_reminder_toggle`
- «✏️ دقیقهٔ یادآور (فعلی: <...>)» → `wh_reminder_set_minutes`
- «🔙 بازگشت» → `menu_pishva`

**منوی حالت تعمیر**  `(kb_repair_menu)`
- «🔧 فعال‌سازی تعمیر» → `repair_on`
- «✅ غیرفعال‌سازی تعمیر» → `repair_off`
- «📝 ثبت دلیل تعمیر» → `repair_reason`
- «🔙 بازگشت» → `menu_pishva`

**تغییر هویت (نام مدیر ارشد/مدیران)**  `(kb_identity)`
- «🪪 تغییر نام مدیر ارشد» → `identity_pishva`
- «👥 تغییر نام مدیران» → `identity_admin`
- «🔙 بازگشت» → `menu_pishva`

**تایید شروع سال تحصیلی جدید**  `(kb_newyear_confirm)`
- «✅ بله، ادامه بده» → `newyear_yes`
- «❌ انصراف» → `menu_pishva`

**منوی آپدیت ربات**  `(kb_update_menu)`
- «💤 خاموشی موقت برای آپدیت» → `update_sleep`
- «📢 اعلام آپدیت» → `update_announce`
- «🔙 بازگشت» → `menu_pishva`

**داشبورد مدیر ارشد**  `(kb_dashboard_pishva)`
- «🔄 به‌روزرسانی» → `dashboard_pishva`
- «🔙 بازگشت» → `back_main`

**تنظیمات بکاپ خودکار**  `(kb_auto_backup_settings)`
- «🔄 بکاپ خودکار» → `abk_toggle`
- «⏰ هر <...> ساعت» → `abk_interval`
- «📁 <...>» → `abk_fmt`
- «📊 بازه: <...>» → `abk_period`
- «🔙 بازگشت» → `pishva_backup`

**بازه‌ی بکاپ خودکار**  `(kb_auto_backup_interval)`
- «⏰ هر ۶ ساعت» → `abk_set_interval_6`
- «⏰ هر ۱۲ ساعت» → `abk_set_interval_12`
- «⏰ هر ۲۴ ساعت» → `abk_set_interval_24`
- «⏰ هر ۴۸ ساعت» → `abk_set_interval_48`
- «🔙 بازگشت» → `pishva_auto_backup`

**وضعیت دیتابیس**  `(kb_dbstatus_menu)`
- «🔙 بازگشت» → `menu_pishva`

**منوی یادآورهای سیستمی**  `(kb_reminders_menu)`
- «فعال‌سازی کلی یادآورها» → `reminder_toggle_master`
- «<...>» → `reminder_toggle_*`
- «⏰ هر <...> ساعت» → `reminder_interval_*`
- «🔙 بازگشت» → `menu_pishva`

**بازه‌ی یادآور**  `(kb_reminder_interval_options)`
- «هر <...> ساعت» → `reminder_set_*_*`
- «🔙 بازگشت» → `pishva_reminders`

**پخش خودکار (گروه/کانال)**  `(kb_broadcast_menu)`
- «— <...> —» → `noop_label`
- «گروه» → `broadcast_toggle_*`
- «کانال» → `broadcast_toggle_*`
- «✏️ ویرایش متنِ اعلان» → `pishva_chess_ai_broadcast_text`
- «🔙 بازگشت» → `menu_pishva`


── لاگ‌ها و پیگیری اقدامات ──

**فیلتر لاگ‌ها (پیگیری اقدامات)**  `(kb_logs_filter)`
- «📅 امروز» → `logs_today`
- «📆 این هفته» → `logs_week`
- «🗓️ این ماه» → `logs_month`
- «📚 کل تاریخ» → `logs_all`
- «🔍 جستجو» → `logs_search`
- «🔙 بازگشت» → `menu_pishva`

**لیست لاگ‌ها**  `(kb_logs_list)`
- «◀️ قبلی» → `logspage_*_*`
- «بعدی ▶️» → `logspage_*_*`
- «🔍 جستجو» → `logs_search`
- «🔙 بازگشت» → `pishva_logs`

**نتایج جستجوی لاگ**  `(kb_logs_search_list)`
- «◀️ قبلی» → `logssearchpage_*`
- «بعدی ▶️» → `logssearchpage_*`
- «🔍 جستجوی جدید» → `logs_search`
- «🔙 بازگشت به فیلتر» → `pishva_logs`

**رد کردن عبارت جستجو**  `(kb_logs_search_skip_term)`
- «⏭️ رد شدن — برو به تنظیم بازه» → `logs_search_skip_term`

**رد کردن بازه‌ی جستجو**  `(kb_logs_search_skip_range)`
- «⏭️ رد شدن — بدون فیلتر ساعت» → `logs_search_skip_range`

**فیلتر لاگ یک مدیر خاص**  `(kb_admin_logs_filter)`
- «📅 امروز» → `adminlogsperiod_*_today`
- «📆 این هفته» → `adminlogsperiod_*_week`
- «🗓️ این ماه» → `adminlogsperiod_*_month`
- «📚 کل تاریخ» → `adminlogsperiod_*_all`
- «🔍 جستجو» → `adminlogssearch_*`
- «🔙 بازگشت» → `admin_view_*`

**لیست لاگ یک مدیر خاص**  `(kb_admin_logs_list)`
- «◀️ قبلی» → `adminlogspg_*_*_*`
- «بعدی ▶️» → `adminlogspg_*_*_*`
- «🔍 جستجو» → `adminlogssearch_*`
- «🔙 بازگشت» → `adminlogsmenu_*`

**نتایج جستجوی لاگ یک مدیر خاص**  `(kb_admin_logs_search_list)`
- «◀️ قبلی» → `adminlogssearchpg_*_*`
- «بعدی ▶️» → `adminlogssearchpg_*_*`
- «🔍 جستجوی جدید» → `adminlogssearch_*`
- «🔙 بازگشت به فیلتر» → `adminlogsmenu_*`


── شطرنج زنده (تاریخچه بازی‌های مدیران) ──

**فیلتر بازی‌های شطرنج مدیران**  `(kb_chess_games_filter)`
- «📅 امروز» → `chessgames_today`
- «📆 این هفته» → `chessgames_week`
- «🗓️ این ماه» → `chessgames_month`
- «📚 کل بازی‌ها» → `chessgames_all`
- «🔙 بازگشت» → `menu_pishva`

**فیلتر تاریخچه شطرنج**  `(kb_chess_history_filter)`
- «📅 امروز» → `chesshist_list_today_all_0`
- «📆 این هفته» → `chesshist_list_week_all_0`
- «🗓️ این ماه» → `chesshist_list_month_all_0`
- «📚 همه بازی‌ها» → `chesshist_list_all_all_0`
- «👤 فیلتر بر اساس مدیر» → `chesshist_admsel_all`
- «🔙 بازگشت» → `chess_menu`

**بازه‌ی تاریخچه شطرنج (با فیلتر مدیر)**  `(kb_chess_history_period_menu)`
- «📅 امروز» → `chesshist_list_today_*_0`
- «📆 این هفته» → `chesshist_list_week_*_0`
- «🗓️ این ماه» → `chesshist_list_month_*_0`
- «📚 کل تاریخ» → `chesshist_list_all_*_0`
- «👤 تغییرِ مدیر» → `chesshist_admsel_all`
- «🔙 بازگشت» → `chess_history`

**انتخاب مدیر برای فیلتر تاریخچه شطرنج**  `(kb_chess_history_admin_select)`
- «🌐 بدون فیلترِ مدیر» → `chesshist_list_*_all_0`
- «🔙 بازگشت» → `chess_history`

**صفحه‌بندی تاریخچه شطرنج**  `(kb_chess_history_nav_row)`
- «◀️ قبلی» → `chesshist_list_*_*_*`
- «بعدی ▶️» → `chesshist_list_*_*_*`

**تغییر فیلتر تاریخچه شطرنج**  `(kb_chess_history_change_rows)`
- «📅 تغییرِ بازه» → `chesshist_periodmenu_*`
- «👤 تغییرِ مدیر» → `chesshist_admsel_*`
- «🔙 بازگشت» → `chess_menu`


── مدیریت دستیار هوشمند (فقط مدیر ارشد) ──

**منوی مدیریت دستیار هوشمند**  `(kb_ai_manage_menu)`
- «🗂️ سوابق چت‌های دستیار» → `ai_admlog_menu`
- «🛠️ اختیارات دستیار» → `ai_perms_menu`
- «🔌 هوش مصنوعی» → `ai_manage_toggle_online`
- «🔕 خاموشی برای ادمین خاص» → `ai_admtg_menu`
- «🔙 بازگشت» → `back_target`

**اختیارات دستیار (روشن/خاموش هر دسته ابزار)**  `(kb_ai_perms_menu)`
- «<...>» → `aiperm_toggle_*`
- «🔙 بازگشت» → `ai_manage_menu_return`

**لیست مدیران برای خاموش/روشن‌کردن دستیار برایشان**  `(kb_ai_admin_toggle_list)`
- «<نام>» → `ai_admtg_pick_*`
- «🔙 بازگشت» → `ai_manage_menu_return`

**روشن/خاموش دستیار برای یک مدیر خاص**  `(kb_ai_admin_toggle_pick)`
- «🟢 روشن باشه» → `ai_admtg_set_*_on`
- «🔴 خاموش باشه» → `ai_admtg_set_*_off`
- «🔙 بازگشت» → `ai_admtg_menu`

**لیست کارهای زمان‌بندی‌شده‌ی دستیار**  `(kb_ai_scheduled_list)`
- «📭 چیزی زمان‌بندی نشده» → `noop_label`
- «❌ لغو» → `aischedcancel_*`
- «🔙 بازگشت» → `menu_pishva`


── هشدار حذف مشکوک / APS (فقط مدیر ارشد) ──

**هشدار حذف مشکوک (real-time alert)**  `(kb_suspicious_alert)`
- «↩️ خنثی‌سازی کل اقدامات این ادمین (امروز)» → `sadel_undo_*_*`
- «🔇 خاموشی ربات برای این ادمین» → `sadel_disable_*`
- «✅ مشکلی نیست» → `sadel_dismiss_*_*`

**تنظیمات کلی هشدار حذف مشکوک**  `(kb_suspicious_settings)`
- «🚨 هشدار حذف مشکوک» → `sadel_toggle`
- «🔢 آستانه: <...> حذف» → `sadel_threshold_menu`
- «⏱️ بازه: <...> دقیقه» → `sadel_window_menu`
- «👤 تنظیمِ جداگانه برای هر ادمین» → `sadel_admins_p0`
- «🔙 بازگشت» → `pishva_settings`

**لیست مدیران برای override تنظیم مشکوک**  `(kb_suspicious_admin_list)`
- «<نام>» → `sadel_admin_*`
- «◀️ قبلی» → `sadel_admins_p*`
- «بعدی ▶️» → `sadel_admins_p*`
- «🔙 بازگشت» → `sadel_panel`

**تنظیم اختصاصی هشدار مشکوک برای یک مدیر**  `(kb_suspicious_admin_panel)`
- «♻️ حذفِ تنظیمِ اختصاصی (بازگشت به کلی)» → `sadel_admin_reset_*`
- «🔙 بازگشت به لیستِ ادمین‌ها» → `sadel_admins_p0`

**آستانه‌ی حذف (اختصاصی مدیر)**  `(kb_suspicious_admin_threshold)`
- «🔢 ۳ حذف» → `sadel_admin_thr_set_*_3`
- «🔢 ۵ حذف» → `sadel_admin_thr_set_*_5`
- «🔢 ۷ حذف» → `sadel_admin_thr_set_*_7`
- «🔢 ۱۰ حذف» → `sadel_admin_thr_set_*_10`
- «↩️ پیروی از تنظیمِ کلی» → `sadel_admin_thr_set_*_def`
- «🔙 بازگشت» → `sadel_admin_*`

**بازه‌ی زمانی (اختصاصی مدیر)**  `(kb_suspicious_admin_window)`
- «⏱️ ۵ دقیقه» → `sadel_admin_win_set_*_5`
- «⏱️ ۱۰ دقیقه» → `sadel_admin_win_set_*_10`
- «⏱️ ۱۵ دقیقه» → `sadel_admin_win_set_*_15`
- «⏱️ ۳۰ دقیقه» → `sadel_admin_win_set_*_30`
- «↩️ پیروی از تنظیمِ کلی» → `sadel_admin_win_set_*_def`
- «🔙 بازگشت» → `sadel_admin_*`

**آستانه‌ی حذف (کلی)**  `(kb_suspicious_threshold)`
- «🔢 ۳ حذف» → `sadel_set_threshold_3`
- «🔢 ۵ حذف» → `sadel_set_threshold_5`
- «🔢 ۷ حذف» → `sadel_set_threshold_7`
- «🔢 ۱۰ حذف» → `sadel_set_threshold_10`
- «🔙 بازگشت» → `sadel_panel`

**بازه‌ی زمانی (کلی)**  `(kb_suspicious_window)`
- «⏱️ ۵ دقیقه» → `sadel_set_window_5`
- «⏱️ ۱۰ دقیقه» → `sadel_set_window_10`
- «⏱️ ۱۵ دقیقه» → `sadel_set_window_15`
- «⏱️ ۳۰ دقیقه» → `sadel_set_window_30`
- «🔙 بازگشت» → `sadel_panel`


── پنل امنیتی APS: صف انتظار و بلاک (مدیر ارشد + مدیر امنیتی) ──

**پنل امنیتی APS (🛡️)**  `(kb_security_panel)`
- «⏳ صف انتظار» → `security_queue`
- «🚫 بلاک‌شده‌ها» → `security_blocked`
- «🔙 بازگشت» → `menu_pishva`

**صف انتظار عضویت**  `(kb_queue_list)`
- «⏳ <نام>» → `queueview_*`
- «🔙 بازگشت» → `security_panel`

**عملیات روی یک درخواست در صف**  `(kb_queue_item_actions)`
- «✅ تأیید و عضویت» → `queueapprove_*`
- «🔓 خروج از صف بدون تأیید» → `queuerelease_*`
- «🚫 بلاک دائم» → `queueblockask_*`
- «🔙 بازگشت» → `security_queue`

**تایید بلاک‌کردن**  `(kb_block_confirm)`
- «🚫 بله، بلاک کن» → `blockconfirm_*`
- «❌ انصراف» → `back_cb`

**لیست بلاک‌شده‌ها**  `(kb_blocked_list)`
- «🚫 <نام>» → `blockedview_*`
- «🔙 بازگشت» → `security_panel`

**عملیات روی یک بلاک‌شده**  `(kb_blocked_item_actions)`
- «🔓 آنبلاک» → `unblock_*`
- «🔙 بازگشت» → `security_blocked`

**درخواست دسترسی (تایید/رد/صف/بلاک)**  `(kb_access_request)`
- «✅ تأیید» → `req_approve_*`
- «❌ رد» → `req_reject_*`
- «⏳ صف انتظار» → `req_queue_*`
- «🚫 بلاک دائم» → `req_blockask_*`


── مدیریت مدیران (فقط مدیر ارشد) ──

**لیست مدیران**  `(kb_admin_list)`
- «<...> <نام>» → `admin_view_*`

**عملیات روی یک مدیر**  `(kb_admin_actions)`
- «🚫 اخراج» → `admin_kick_*`
- «🔄 احیا» → `admin_revive_*`
- «⬆️ دسترسی‌ها» → `admin_perms_*`
- «⚠️ ثبت اخطار» → `admin_warn_*`
- «🧹 پاک‌کردن اخطارها» → `admin_clearwarn_*`
- «💬 ارسال پیام» → `admin_msg_*`
- «📋 اعطای وظیفه» → `admin_task_*`
- «🔍 پیگیری اقدامات» → `adminlogsmenu_*`
- «👁️ پروفایل» → `admin_profile_*`
- «🔙 بازگشت» → `menu_admins`

**دسترسی‌های تفصیلی یک مدیر (روشن/خاموش)**  `(kb_admin_permissions)`
- «🔔 اعلان» → `perm_*_notifications`
- «📰 اخبار» → `perm_*_news`
- «♟️ مسابقات» → `perm_*_match_management`
- «👥 بازیکنان» → `perm_*_view_players`
- «⚠️ اخطار» → `perm_*_issue_warning`
- «🚫 درخواست اخراج» → `perm_*_request_ban`
- «❌ اخراج مستقیم» → `perm_*_direct_ban`
- «📋 وظیفه» → `perm_*_assign_task`
- «🚨 گزارش» → `perm_*_report`
- «💤 ربات فعال» → `perm_*_bot_active`
- «⚙️ تنظیمات» → `perm_*_settings_access`
- «🌟 ارشد» → `perm_*_senior_admin`
- «✏️ ویرایش مسابقه» → `perm_*_edit_delete_match`
- «📡 مخابرات» → `perm_*_communications`
- «🤖 دسترسی هوش مصنوعی» → `perm_*_ai_access`
- «♟️ شطرنج زنده» → `perm_*_chess_access`
- «✅ ذخیره و بازگشت» → `admin_view_*`


── مخابرات ──

**منوی مخابرات — مدیر ارشد**  `(kb_comms_pishva)`
- «💬 پیام به ادمین» → `comms_msg_admin`
- «📢 ارسال بیانیه» → `comms_announce`
- «📨 پیام‌های دریافتی» → `comms_inbox`
- «🔔 اعلانات اخیر» → `comms_notifs`
- «👁️ پیام ادمین‌ها» → `comms_all_msgs`
- «📰 ارسال خبر» → `comms_news`
- «📊 گزارشات» → `comms_reports`
- «📜 تاریخچه بیانیات» → `comms_ann_history`
- «🔙 بازگشت» → `back_main`

**منوی مخابرات — مدیر معمولی**  `(kb_comms_admin)`
- «💬 پیام به مدیر ارشد» → `comms_msg_pishva`
- «💬 پیام به ادمین» → `comms_msg_other`
- «📨 پیام‌های دریافتی» → `comms_inbox`
- «📜 بیانیات» → `comms_ann_history`
- «📰 اخبار» → `comms_news_list`
- «🔙 بازگشت» → `back_main`

**پیوست‌کردن فایل به بیانیه؟**  `(kb_announce_file)`
- «📎 بله، پیوست کن» → `ann_with_file`
- «➡️ خیر، فقط متن» → `ann_no_file`


── وظایف ──

**منوی وظایف — مدیر ارشد**  `(kb_tasks_pishva)`
- «📋 اعطای وظیفه» → `task_assign`
- «📜 تاریخچه وظایف» → `task_history`
- «🔙 بازگشت» → `back_main`

**منوی وظایف — مدیر معمولی**  `(kb_tasks_admin)`
- «📌 پیگیری وظایف» → `task_track`
- «🔙 بازگشت» → `back_main`

**علامت‌زدن وضعیت وظیفه**  `(kb_task_status)`
- «✅ انجام شد» → `task_done_*`
- «❌ انجام نشد» → `task_fail_*`

**فیلتر تاریخچه‌ی وظایف**  `(kb_task_history_filter)`
- «📅 امروز» → `thistory_today`
- «📆 این هفته» → `thistory_week`
- «✅ انجام‌شده» → `thistory_done`
- «❌ انجام‌نشده» → `thistory_pending`
- «🔙 بازگشت» → `menu_tasks`


── انتقادات و پیشنهادات ──

**منوی ثبت انتقاد/پیشنهاد**  `(kb_feedback_menu)`
- «📝 انتقاد» → `fb_critique`
- «💡 پیشنهاد» → `fb_suggestion`
- «🏆 تقدیر» → `fb_praise`
- «🔧 درخواست قابلیت» → `fb_feature`
- «🔙 بازگشت» → `back_main`

**مشاهده‌ی انتقادات/پیشنهادات (مدیر ارشد)**  `(kb_feedback_pishva)`
- «📝 انتقادات» → `fb_view_critique`
- «💡 پیشنهادات» → `fb_view_suggestion`
- «🏆 تقدیرها» → `fb_view_praise`
- «🔧 قابلیت‌ها» → `fb_view_feature`
- «📋 همه موارد» → `fb_view_all`
- «🔙 بازگشت» → `back_main`


── راهنما ──

**منوی راهنما (دسته‌بندی موضوعی)**  `(kb_help_menu)`
- «🏅 تورنمنت» → `help_tournament`
- «👤 بازیکنان» → `help_players`
- «♟️ مسابقات» → `help_matches`
- «📡 مخابرات» → `help_comms`
- «⚠️ اخطار» → `help_warnings`
- «📋 وظایف» → `help_tasks`
- «❓ سوالات متداول» → `help_faq`
- «🛠️ خطاهای احتمالی» → `help_errors`
- «🔙 بازگشت» → `back_main`


── داشبورد ادمین معمولی ──

**داشبورد مدیر معمولی**  `(kb_dashboard_admin)`
- «🔄 به‌روزرسانی» → `dashboard_admin`
- «🔙 بازگشت» → `back_main`


── ثبت‌نام گروهی ──

**پیش‌نمایش ثبت‌نام گروهی**  `(kb_bulk_preview)`
- «✅ ثبت و تایید» → `bulk_confirm`
- «✏️ ویرایش» → `bulk_edit`
- «❌ لغو» → `bulk_cancel`


── عمومی / مشترک ──

**انتخاب نقش (اولین ورود)**  `(kb_role_select)`
- «👑 رییس کل» → `role_pishva`
- «🏆 مدیر مسابقات» → `role_tournament`
- «🛡️ مدیر امنیتی» → `role_security`

**دکمه‌ی بازگشت**  `(kb_back)`
- «🔙 بازگشت» → `back_*`

**ردیف بازگشت**  `(kb_back_row)`
- «🔙 بازگشت» → `back_*`

**دکمه‌ی انصراف**  `(kb_cancel)`
- «❌ انصراف» → `cb`

**منوی وقتی پنل بسته شده**  `(kb_panel_closed_menu)`
- «🔍 لاگ‌ها» → `pishva_logs`
- «🔓 ورود به پنل» → `back_main`

**تایید/رد عمومی**  `(kb_confirm)`
  _(بدون دکمه ثابت — پویا/فقط بازگشت)_

---

## ۳) دستورات کلمه‌ای (Keyword Commands)

کاربر می‌تواند به‌جای زدن دکمه، همین کلمه را مستقیم در چت (پیوی یا گروه) تایپ کند تا همان
پنل/عملیات باز شود. اگر کلمه در ستون «مجاز برای» فقط «مدیر ارشد» باشد، برای بقیه‌ی نقش‌ها اثری ندارد
(پیام خطای دسترسی می‌گیرند).

| کلمه‌ی گفته‌شده | عملیات | توضیح | مجاز برای |
|---|---|---|---|
| «پنل» | `quick_panel` | باز کردن پنل سریع (بر اساس نقش کاربر) | همه‌ی نقش‌ها |
| «داشبورد» | `dashboard` | باز کردن داشبورد | همه‌ی نقش‌ها |
| «وظیفه» | `tasks` | منوی وظایف | همه‌ی نقش‌ها |
| «مسابقه» | `matches` | منوی مدیریت مسابقات | همه‌ی نقش‌ها |
| «بازیکن» | `players` | منوی مدیریت بازیکنان | همه‌ی نقش‌ها |
| «مخابره» | `comms` | منوی مخابرات | همه‌ی نقش‌ها |
| «امنیت» | `security` | پنل امنیتی APS | فقط مدیر ارشد |
| «وضعیت» | `status` | مدیریت وضعیت سیستم | فقط مدیر ارشد |
| «کلاس» | `classes` | مدیریت کلاس‌ها | همه‌ی نقش‌ها |
| «تیم» | `teams` | منوی تیم‌ها | همه‌ی نقش‌ها |
| «بکاپ» | `backup` | منوی بکاپ | فقط مدیر ارشد |
| «درخواست» | `requests` | درخواست‌های دسترسی | فقط مدیر ارشد |
| «لاگ» | `logs` | لاگ‌ها/پیگیری اقدامات | فقط مدیر ارشد |
| «گزارش» | `logs` | لاگ‌ها/پیگیری اقدامات | فقط مدیر ارشد |
| «راهنما» | `help` | راهنما | همه‌ی نقش‌ها |
| «کمک» | `help` | راهنما | همه‌ی نقش‌ها |
| «قرعه» | `lottery` | قرعه‌کشی | همه‌ی نقش‌ها |
| «جدول» | `elo` | جدول Elo | همه‌ی نقش‌ها |
| «رتبه» | `elo` | جدول Elo | همه‌ی نقش‌ها |
| «قهرمانان» | `champions` | قهرمانان | همه‌ی نقش‌ها |
| «یادآور» | `reminders` | یادآورهای سیستمی | فقط مدیر ارشد |
| «تنظیمات» | `settings` | تنظیمات ربات | فقط مدیر ارشد |
| «فیدبک» | `feedback` | انتقادات و پیشنهادات | همه‌ی نقش‌ها |
| «انتقاد» | `feedback` | انتقادات و پیشنهادات | همه‌ی نقش‌ها |
| «شروع» | `restart` | خوش‌آمدگویی کامل (/start) | همه‌ی نقش‌ها |
| «بستن» | `close_panel` | بستن پنل باز | همه‌ی نقش‌ها |
| «خروج» | `close_panel` | بستن پنل باز | همه‌ی نقش‌ها |
| «مدیر ارشد» | `pishva_panel` | پنل مدیر ارشد (فقط خودش) | فقط مدیر ارشد |
| «مدیریت» | `restart` | خوش‌آمدگویی کامل (/start) | همه‌ی نقش‌ها |
| «دست» | `ai_manage_panel` | پنل مدیریت دستیار هوشمند | فقط مدیر ارشد |
| «رصد» | `ai_manage_panel` | پنل مدیریت دستیار هوشمند | فقط مدیر ارشد |
| «اطلاعات» | `reply_info` | جزئیات کاربر (با ریپلای روی پیامش) | همه‌ی نقش‌ها |
| «درباره» | `reply_info` | جزئیات کاربر (با ریپلای روی پیامش) | همه‌ی نقش‌ها |
| «کیه» | `reply_info` | جزئیات کاربر (با ریپلای روی پیامش) | همه‌ی نقش‌ها |
| «آنلاین» | `online_admins` | لیست مدیران آنلاین/فعال | فقط مدیر ارشد |
| «الان» | `online_admins` | لیست مدیران آنلاین/فعال | فقط مدیر ارشد |
| «ادمین‌ها» | `admin_list` | لیست همه‌ی مدیران | همه‌ی نقش‌ها |
| «ادمینا» | `admin_list` | لیست همه‌ی مدیران | همه‌ی نقش‌ها |
| «مدیران» | `admin_list` | لیست همه‌ی مدیران | همه‌ی نقش‌ها |
| «آمار» | `quick_stats` | آمار سریع بازیکنان و مسابقات | همه‌ی نقش‌ها |
| «نتایج» | `recent_results` | آخرین نتایج مسابقات | همه‌ی نقش‌ها |
| «اخطارها» | `warnings_list` | لیست بازیکنان دارای اخطار | همه‌ی نقش‌ها |
| «بازی» | `live_chess` | منوی شطرنج زنده | همه‌ی نقش‌ها |
| «شطرنج» | `live_chess` | منوی شطرنج زنده | همه‌ی نقش‌ها |
| «ساعت» | `workhours_menu` | منوی ساعت کاری | فقط مدیر ارشد |
| «ساعت کاری» | `workhours_menu` | منوی ساعت کاری | فقط مدیر ارشد |
| «کاری» | `workhours_menu` | منوی ساعت کاری | فقط مدیر ارشد |
| «زمان» | `workhours_menu` | منوی ساعت کاری | فقط مدیر ارشد |

**کلمات ویژه‌ی تنظیم/حذف مدیر (فقط مدیر ارشد):** «تنظیم مدیر»، «تنظیم مدیر امنیتی»، «حذف مدیر»، «حذف مدیر امنیتی»

**شروع ساعت کاری (فقط مدیر ارشد، وارد گفتگوی جدا می‌شود):** «آغاز»، «اغاز»
**پایان ساعت کاری (فقط مدیر ارشد):** «تموم»، «پایان»

نکات تکمیلی:
- در **گروه/سوپرگروه**، وقتی این کلمات گفته شوند، ابتدا از کاربر پرسیده می‌شود پنل «همینجا (گروه)»
  باز شود یا در «پیوی (امن‌تر)» — چون پنل باز‌شده در گروه فقط برای همان گفتاورنده قابل‌کلیک است
  (قفل مالکیت پنل) و بعد از چند دقیقه بی‌فعالیتی خودکار بسته می‌شود.
- روی هر پیام دیگری در گروه اگر ادمین «ریپلای» کند و بنویسد «اطلاعات»/«درباره»/«کیه»، جزئیاتِ همان
  کاربر نمایش داده می‌شود.
- مدیر ارشد می‌تواند با کلماتِ «تنظیم مدیر»، «تنظیم مدیر امنیتی»، «حذف مدیر»، «حذف مدیر امنیتی»
  (روی ریپلایِ پیامِ فرد موردنظر) مستقیم نقش بدهد/بگیرد، بدون رفتن به «مدیریت مدیران».


---

## ۴) ابزارهایی که خودِ دستیار هوشمند مستقیم اجرا می‌کند

این‌ها را دستیار به‌جای هدایتِ کاربر به یک دکمه، خودش با «فراخوانی تابع» (function calling)
انجام می‌دهد — یعنی کاربر لازم نیست وارد هیچ منویی شود، کافی‌ست در چت با دستیار بخواهد.

| ابزار (تابعی که دستیار مستقیم صدا می‌زند) | کاربرد | نقش‌های مجاز |
|---|---|---|
| `start_workhours` | شروع ساعت کاری | مدیر ارشد |
| `end_workhours` | پایان ساعت کاری | مدیر ارشد |
| `register_player` | ثبت‌نام بازیکن جدید | مدیر ارشد، مدیر مسابقات |
| `search_player` | جستجوی بازیکن | همه‌ی نقش‌ها |
| `warn_player` | ثبت اخطار برای بازیکن | مدیر ارشد، مدیر مسابقات |
| `kick_player` | اخراج بازیکن | مدیر ارشد، مدیر مسابقات |
| `revive_player` | احیای بازیکن حذف/تعلیق‌شده | مدیر ارشد، مدیر مسابقات |
| `create_tournament` | ساخت تورنمنت جدید | مدیر ارشد، مدیر مسابقات |
| `list_tournaments` | لیست تورنمنت‌ها | همه‌ی نقش‌ها |
| `analyze_tournament` | تحلیل یک تورنمنت | همه‌ی نقش‌ها |
| `record_match` | ثبت مسابقه/نتیجه | مدیر ارشد، مدیر مسابقات |
| `recent_matches` | آخرین مسابقات | همه‌ی نقش‌ها |
| `quick_stats` | آمار سریع | همه‌ی نقش‌ها |
| `system_status` | وضعیت کلی سیستم/ربات | همه‌ی نقش‌ها |
| `check_security` | بررسی وضعیت امنیتی | مدیر ارشد |
| `send_announcement` | ارسال بیانیه | مدیر ارشد |
| `send_news` | ارسال خبر | مدیر ارشد |
| `message_admin` | ارسال پیام مستقیم به یک مدیر خاص | مدیر ارشد |
| `assign_task` | اعطای وظیفه به یک مدیر | مدیر ارشد |
| `remember_note` | ثبت یادداشت در حافظه‌ی بلندمدت دستیار | مدیر ارشد |
| `recall_notes` | بازیابی یادداشت‌های حافظه | مدیر ارشد |
| `forget_note` | حذف یک یادداشت از حافظه | مدیر ارشد |
| `list_admins` | لیست مدیران | مدیر ارشد، مدیر امنیتی |
| `review_admins_activity` | بررسی فعالیت مدیران | مدیر ارشد |
| `warn_admin` | ثبت اخطار برای یک مدیر | مدیر ارشد |
| `clear_admin_warnings` | پاک‌کردن اخطارهای یک مدیر | مدیر ارشد |
| `set_admin_role` | تغییر نقش یک مدیر / تنظیم مدیر جدید | مدیر ارشد |
| `block_user` | بلاک‌کردن کاربر | مدیر ارشد، مدیر امنیتی |
| `unblock_user` | آنبلاک‌کردن کاربر | مدیر ارشد، مدیر امنیتی |
| `open_panel` | باز کردن یک پنل (دکمه‌ی شیشه‌ای) | همه‌ی نقش‌ها |
| `edit_match_result` | اصلاح نتیجه‌ی ثبت‌شده‌ی یک مسابقه | مدیر ارشد |
| `delete_match` | حذف یک مسابقه | مدیر ارشد |
| `get_admin_profile` | مشاهده‌ی پروفایل کامل یک مدیر | مدیر ارشد |
| `set_system_status` | تغییر وضعیت سیستم (نرمال/بد/خطرناک/APS) | مدیر ارشد |
| `toggle_ai_online` | روشن/خاموش‌کردن کلی دستیار هوشمند | مدیر ارشد |
| `toggle_admin_ai_access` | روشن/خاموش‌کردن دستیار برای یک مدیر خاص | مدیر ارشد |
| `toggle_bot_setting` | روشن/خاموش‌کردن یک تنظیم کلی ربات | مدیر ارشد |
| `set_reminder` | تنظیم یادآور شخصی | همه‌ی نقش‌ها |
| `schedule_action` | زمان‌بندی اجرای یک عملیات در آینده | همه‌ی نقش‌ها |
| `list_scheduled` | لیست کارهای زمان‌بندی‌شده | همه‌ی نقش‌ها |
| `cancel_scheduled` | لغو یک کار زمان‌بندی‌شده | همه‌ی نقش‌ها |
| `batch_execute` | اجرای دسته‌ای/تکراری چند عملیات با هم | همه‌ی نقش‌ها |

نکته: `ALL_ROLES` یعنی هر سه نقش (مدیر ارشد، مدیر مسابقات، مدیر امنیتی). برای عملیاتِ سنگین/تکراری
(مثلاً «۵۰ بازیکن ثبت کن») دستیار به‌جای صدا زدن پی‌درپیِ یک تابع، از `batch_execute` استفاده می‌کند.


---

## ۵) راهنمای گام‌به‌گام کامل هر بخش (محتوای مرکز راهنمای داخل ربات)

این بخش عیناً همان محتوایی است که در ربات از مسیر «راهنما» یا با کلمه‌ی «راهنما»/«کمک» دیده
می‌شود؛ شاملِ مسیر دقیق دکمه‌ها برای رسیدن به هر کار است. دستیار باید این «📌 مسیر:»ها را دقیقاً
عیناً به کاربر بگوید، نه یک نسخه‌ی حدسی از خودش.

======================================================================
SECTION: quick_start — 🚀 شروع سریع
======================================================================
--- step 1: قدم ۱ — ثبت کلاس ---
♟️ *سیستم فرماندهی شطرنج*
━━━━━━━━━━━━━━━━━━━━

🚀 *شروع سریع — قدم ۱ از ۶*

🏫 *ثبت کلاس*

ابتدا باید کلاس‌های مدرسه را ثبت کنید.

📌 مسیر:
منوی اصلی ← مدیریت بازیکنان ← مدیریت کلاس‌ها ← ثبت کلاس جدید

✏️ فقط نام کلاس را وارد کنید (مثلاً ۹۰۱ یا ۱۰۲)

💡 *نکته:* بدون کلاس نمیتوان بازیکن ثبت کرد.

--- step 2: قدم ۲ — ثبت بازیکنان ---
♟️ *سیستم فرماندهی شطرنج*
━━━━━━━━━━━━━━━━━━━━

🚀 *شروع سریع — قدم ۲ از ۶*

👤 *ثبت بازیکنان*

پس از ساختن کلاس، بازیکنان را ثبت کنید.

📌 مسیر:
مدیریت بازیکنان ← ثبت‌نام بازیکن

مراحل:
1️⃣ کلاس بازیکن را انتخاب کنید
2️⃣ نام و نام‌خانوادگی را وارد کنید

✅ بازیکن ثبت می‌شود و به لیست ادامه‌دهندگان اضافه می‌شود.

💡 *برای ثبت گروهی چند نفر با هم:* راهنمای «📋 ثبت‌نام گروهی» را ببینید.

--- step 3: قدم ۳ — ساخت تورنمنت ---
♟️ *سیستم فرماندهی شطرنج*
━━━━━━━━━━━━━━━━━━━━

🚀 *شروع سریع — قدم ۳ از ۶*

🏅 *ساخت تورنمنت*

📌 مسیر:
مدیریت تورنمنت ← افزودن تورنمنت

فقط نام تورنمنت را وارد کنید.

بعد از ساخت:
⚙️ مدیریت تورنمنت ← انتخاب تورنمنت ← تنظیم پیش‌فرض

💡 *نکته:* مسابقات به تورنمنت پیش‌فرض اضافه می‌شوند.

--- step 4: قدم ۴ — ثبت مسابقه ---
♟️ *سیستم فرماندهی شطرنج*
━━━━━━━━━━━━━━━━━━━━

🚀 *شروع سریع — قدم ۴ از ۶*

♟️ *ثبت مسابقه*

📌 مسیر:
مدیریت مسابقات ← ثبت مسابقه جدید

مراحل:
1️⃣ بازیکن سفید را از لیست انتخاب کنید
2️⃣ بازیکن سیاه را انتخاب کنید
3️⃣ تاریخ را وارد کنید (یا امروز را بزنید)

🎲 *یا از قرعه‌کشی هوشمند استفاده کنید!*
سیستم خودکار دو بازیکن مناسب انتخاب می‌کند.

--- step 5: قدم ۵ — ثبت نتیجه ---
♟️ *سیستم فرماندهی شطرنج*
━━━━━━━━━━━━━━━━━━━━

🚀 *شروع سریع — قدم ۵ از ۶*

🏆 *ثبت نتیجه*

📌 مسیر:
مدیریت مسابقات ← ثبت نتیجه

گزینه‌ها:
🥇 برد سفید
🥇 برد سیاه
🤝 تساوی (علت پرسیده می‌شود)

پس از ثبت نتیجه، سیستم می‌پرسد آیا بازنده حذف شود یا نه.

💡 اگر گروه یا کانال اعلانات تنظیم شده باشد، نتیجه خودکار به آن‌جا هم ارسال می‌شود.

--- step 6: قدم ۶ — مشاهده آمار ---
♟️ *سیستم فرماندهی شطرنج*
━━━━━━━━━━━━━━━━━━━━

🚀 *شروع سریع — قدم ۶ از ۶*

📊 *مشاهده آمار*

آمار بازیکنان:
مدیریت بازیکنان ← مشاهده بازیکنان ← کلیک روی هر بازیکن

پنل مسابقات:
مدیریت مسابقات ← پنل مدیریت

جزئیات تورنمنت:
مدیریت تورنمنت ← جزئیات تورنمنت فعال

داشبورد کامل مدیر ارشد:
منوی اصلی ← داشبورد مدیر ارشد

🎉 *تبریک! سیستم را یاد گرفتید.*

======================================================================
SECTION: matches — 🏆 مدیریت مسابقات
======================================================================
--- step 1: ثبت مسابقه ---
♟️ *راهنمای مسابقات — ثبت مسابقه*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:*
مدیریت مسابقات ← ثبت مسابقه جدید

⚠️ *شرایط لازم:*
• حداقل ۲ بازیکن ادامه‌دهنده باید وجود داشته باشد
• بازیکنان نباید تعلیق یا اخراج باشند
• بازیکنان نباید ۳ اخطار داشته باشند

🎲 *قرعه‌کشی هوشمند:*
سیستم سعی می‌کند بازیکنانی انتخاب کند که قبلاً با هم بازی نداشتند.

--- step 2: ثبت نتیجه ---
♟️ *راهنمای مسابقات — ثبت نتیجه*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:*
مدیریت مسابقات ← ثبت نتیجه

🏆 *گزینه‌های نتیجه:*
• 🥇 برد سفید
• 🥇 برد سیاه
• 🤝 تساوی

📋 *علل تساوی:*
پات | اتمام زمان | حرکات بسیار | سه تکرار | سایر

⚠️ *هشدار:* اگر بازنده 🌟 برتر یا ⚡ ویژه باشد، هشدار نمایش داده می‌شود.

--- step 3: تاریخچه و جستجو ---
♟️ *راهنمای مسابقات — تاریخچه*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:*
مدیریت مسابقات ← جستجو/تاریخچه

🔍 *فیلترها:*
• 📅 امروز
• 📆 این هفته
• 🗓️ این ماه
• 📚 کل مسابقات
• 🔍 جستجو با نام یا تاریخ

با کلیک روی هر مسابقه:
✏️ ویرایش | 🗑️ حذف | 📌 پین کردن

--- step 4: قرعه‌کشی پیشرفته ---
🎯 *قرعه‌کشی پیشرفته*
━━━━━━━━━━━━━━━━━━━━

برای زمانی که می‌خواهید چند مسابقه را با هم و به‌صورت شانسی بسازید (مثلاً کل یک فصل مسابقات).

📌 *مسیر:*
مدیریت مسابقات ← 🎯 قرعه‌کشی پیشرفته

۱. محدوده را انتخاب کنید:
   🎯 هر دو طرف از یک کلاس
   🔀 هرکدام از یک کلاس متفاوت (مثلاً یک طرف از ۹۰۱، طرف دیگر از ۷۰۲)
   🌐 از همه کلاس‌ها (بدون محدودیت)

۲. اگر «کلاس متفاوت» را انتخاب کردید، هر دو کلاس را جدا مشخص می‌کنید.

۳. تعداد نفراتی که می‌خواهید شانسی رقابت کنند را بفرستید (باید زوج باشد؛ مثلاً ۲۰ نفر یعنی ۱۰ مسابقه ساخته می‌شود).

--- step 5: قرعه‌کشی پیشرفته — پیش‌نمایش و هشدار کمبود ---
🎯 *قرعه‌کشی پیشرفته — ادامه*
━━━━━━━━━━━━━━━━━━━━

پس از وارد کردن تعداد، پیش‌نمایش تمام مسابقات ساخته‌شده نشان داده می‌شود.

✅ *ثبت همه* — همه‌ی مسابقات یک‌جا ثبت می‌شوند
🔄 *دوباره* — قرعه‌کشی با همان تنظیمات دوباره انجام می‌شود
❌ *لغو* — هیچ‌چیز ثبت نمی‌شود

⚠️ *اگر تعداد درخواستی از تعداد بازیکنان موجود (در آن کلاس یا کلاس‌ها) بیشتر باشد،* ربات هشدار می‌دهد و دقیقاً می‌گوید چند نفر کم دارید — می‌توانید عدد کوچک‌تری بفرستید.

======================================================================
SECTION: keywords — 🗣️ دستورات کلمه‌ای
======================================================================
--- step 1: چیست ---
🗣️ *دستورات کلمه‌ای*
━━━━━━━━━━━━━━━━━━━━

علاوه بر دکمه‌ها، می‌توانید با نوشتن یک کلمه‌ی ساده (بدون /) مستقیماً پنل موردنظر را باز کنید — نیازی به منوگردی نیست.

این کلمات فقط برای مدیر ارشد و مدیران فعال کار می‌کنند.

💡 *نکته گروه:* وقتی توی گروه پنل باز می‌کنید، آن پنل فقط *متعلق به شماست* — اگر کس دیگری دست بزند هشدار می‌گیرد.

--- step 2: کلمات باز کردن پنل‌ها ---
🗣️ *کلمات باز کردن پنل‌ها*
━━━━━━━━━━━━━━━━━━━━

`پنل` — پنل اصلی خودتان را باز می‌کند
`داشبورد` — داشبورد کامل شما را نشان می‌دهد
`وظیفه` — پنل وظایف را باز می‌کند
`مسابقه` — پنل مدیریت مسابقات را باز می‌کند
`بازیکن` — پنل مدیریت بازیکنان را باز می‌کند
`مخابره` — پنل مخابرات را باز می‌کند
`کلاس` — مدیریت کلاس‌ها را باز می‌کند
`تیم` — پنل مدیریت تیم‌ها (اگر حالت تیمی فعال باشد)
`قرعه` — منوی قرعه‌کشی هوشمند را باز می‌کند
`راهنما` یا `کمک` — همین واحد راهنما را باز می‌کند
`فیدبک` یا `انتقاد` — پنل انتقادات و پیشنهادات
`شروع` — صفحه‌ی خوش‌آمد خودتان را دوباره نشان می‌دهد

💡 هرکدام از این‌ها همان پنلی را باز می‌کند که با دکمه هم بازش می‌کردید.

--- step 3: کلمات اطلاعاتی ---
🗣️ *کلمات اطلاعاتی*
━━━━━━━━━━━━━━━━━━━━

`جدول` یا `رتبه` — جدول رتبه‌بندی Elo (۱۰ نفر برتر)
`قهرمانان` — قهرمان هفته و قهرمان ماه
`آمار` — آمار سریع بازیکنان و مسابقات
`نتایج` — آخرین ۱۰ نتیجه‌ی مسابقات
`اخطارها` — لیست بازیکنانی که اخطار دارند
`مدیران` یا `ادمین‌ها` یا `ادمینا` — لیست مدیران فعال
`آنلاین` — مدیرانی که در ۲۴ ساعت اخیر فعال بودند

--- step 4: ریپلای اطلاعات کاربر ---
🗣️ *ریپلای روی پیام → اطلاعات کاربر*
━━━━━━━━━━━━━━━━━━━━

روی پیام هر کسی ریپلای کنید و یکی از این کلمات را بنویسید:

`اطلاعات` — اطلاعات کامل آن شخص
`درباره` — مترادف اطلاعات
`کیه` — مترادف اطلاعات

💡 *اگر پیامی از خودِ آن شخص در دسترس نیست:* کافیست آیدیِ عددی او را خودتان در یک پیام بفرستید و روی همان پیام (که خودتان فرستادید) ریپلای کرده و «اطلاعات» را بزنید — ربات همان‌طور کار می‌کند.

📋 *اطلاعاتی که نمایش داده می‌شود:*
• نقش در سیستم (مدیر ارشد / مدیر / کاربر عادی)
• آخرین زمان فعالیت
• دسترسی‌های فعال و غیرفعال
• تاریخ عضویت
• وضعیت بلاک بودن در APS
• اخطارهای مدیریتی

🕓 *اگر آن شخص قبلاً با ربات کار کرده ولی الان ادمین نیست* (یا حتی اگر قبلاً ادمین بوده و الان اخراج شده)، سابقه‌ی فعالیتش (اولین/آخرین فعالیت، تعداد اقدامات) و آخرین نام/یوزرنیمِ شناخته‌شده هم نشان داده می‌شود. تلگرام وضعیت آنلاین/آفلاین لحظه‌ای را برای ربات‌ها در اختیار نمی‌گذارد؛ فقط دسترسی فعلیِ ربات به او (بلاک/آنبلاک) و آخرین فعالیتِ ثبت‌شده روی همین ربات قابل نمایش است.

--- step 5: کلمات مخصوص مدیر ارشد ---
🗣️ *کلمات مخصوص مدیر ارشد*
━━━━━━━━━━━━━━━━━━━━

`مدیر ارشد` — پنل فرماندهی مدیر ارشد را باز می‌کند (فقط برای مدیر ارشد)
`امنیت` — منوی انتخاب وضعیت سیستم (نرمال/بد/خطرناک/APS)
`وضعیت` — گزارش کامل و آنی از کل سیستم
`بکاپ` — مستقیم به سراغ گرفتن پشتیبان می‌رود
`درخواست` — درخواست‌های دسترسی در انتظار
`لاگ` یا `گزارش` — لاگ اقدامات امروز
`یادآور` — پنل تنظیمات یادآورها
`تنظیمات` — پنل تنظیمات ربات

⚠️ این کلمات فقط برای مدیر ارشد کار می‌کنند.

--- step 6: بیانیه و خبر (فقط مدیر ارشد) ---
🗣️ *بیانیه و خبر*
━━━━━━━━━━━━━━━━━━━━

`بیانیه` — مستقیم شروع نوشتن یک بیانیه‌ی رسمی می‌کند

`خبر` — مستقیم شروع نوشتن یک خبر فوری می‌کند

بعد از فرستادن این کلمه، ربات متن بیانیه/خبر را از شما می‌پرسد — دقیقاً مثل وقتی از منو وارد می‌شدید.

--- step 7: تنظیم و حذف مدیر با ریپلای ---
🗣️ *تنظیم/حذف مدیر با ریپلای*
━━━━━━━━━━━━━━━━━━━━

توی یک گروه یا کانالی که ربات عضوشه، روی پیام شخص موردنظر ریپلای کنید و یکی از این کلمات رو بفرستید:

`تنظیم مدیر` — او را 🏆 مدیر مسابقات می‌کند
`تنظیم مدیر امنیتی` — او را 🛡️ مدیر امنیتی می‌کند
`حذف مدیر` / `حذف مدیر امنیتی` — دسترسی مدیریتی او را حذف می‌کند

⚠️ *نکته‌ی مهم:* برای اینکه ربات بتواند پیام‌های داخل گروه را ببیند، باید یا ربات در آن گروه ادمین باشد، یا حالت Privacy Mode آن در BotFather (دستور /setprivacy) غیرفعال شده باشد.

فقط مدیر ارشد می‌تواند این دستورات را استفاده کند.

======================================================================
SECTION: players — ♟️ مدیریت بازیکنان
======================================================================
--- step 1: ثبت و مدیریت ---
♟️ *راهنمای بازیکنان — ثبت*
━━━━━━━━━━━━━━━━━━━━

📌 *ثبت بازیکن:*
۱. ابتدا کلاس بسازید
۲. مدیریت بازیکنان ← ثبت‌نام بازیکن
۳. کلاس را انتخاب کنید
۴. نام را وارد کنید

📊 *وضعیت‌های بازیکن:*
🟢 فعال | ⏸️ تعلیق | ❌ اخراج | ⛔ حذف در مسابقه

--- step 2: پنل بازیکن ---
♟️ *راهنمای بازیکنان — پنل*
━━━━━━━━━━━━━━━━━━━━

با کلیک روی نام هر بازیکن پنل کامل باز می‌شود:

📊 *اطلاعات نمایش داده‌شده:*
• رتبه: 🔰 مبتدی تا 👑 افسانه‌ای
• آمار برد/مساوی/باخت
• درصد برد (نوار گرافیکی)
• سطح قدرت
• بهترین حریف و سخت‌ترین حریف
• وضعیت اخطار
• امتیاز Elo و عنوان رسمی

🎮 *دکمه‌های عملیاتی:*
✏️ ویرایش | ⚠️ اخطار | 🚫 اخراج | ⏸️ تعلیق | 🔄 احیا | 📝 یادداشت

--- step 3: سیستم اخطار ---
♟️ *راهنمای بازیکنان — اخطار*
━━━━━━━━━━━━━━━━━━━━

⚠️ *حداکثر اخطار بازیکن: ۳*

نمایش اخطار:
[ 🟢 ░ ░ ] — بدون اخطار
[ 🟢 🟠 ░ ] — یک اخطار
[ 🟢 🟠 🔴 ] — دو اخطار
[ 🟢 🟠 🔴 ] — سه اخطار (خطرناک)

با رسیدن به ۳ اخطار، مدیر ارشد مطلع می‌شود و تصمیم می‌گیرد:
حذف | تعلیق | ادامه | محدودیت

--- step 4: امتیاز Elo و رتبه‌بندی ---
📈 *راهنمای بازیکنان — امتیاز Elo*
━━━━━━━━━━━━━━━━━━━━

هر بازیکن یک امتیاز Elo دارد که بعد از هر مسابقه خودکار به‌روزرسانی می‌شود (برد امتیاز می‌گیرد، باخت از دست می‌دهد؛ میزان تغییر به قدرت حریف بستگی دارد).

📌 *مشاهده:*
• جدول کامل رتبه‌بندی: منوی اصلی ← 📊 جدول Elo
• روی پنل هر بازیکن: امتیاز و عنوان رسمی (🔰 مبتدی تا 👑 افسانه‌ای)

💡 عنوان رسمی بازیکن مستقیماً از روی امتیاز Elo تعیین می‌شود.

--- step 5: پیش‌بینی نتیجه مسابقه ---
🔮 *راهنمای بازیکنان — پیش‌بینی*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:*
پنل بازیکن ← 🔮 پیش‌بینی ← انتخاب حریف

بر اساس امتیاز Elo دو طرف، ربات نشان می‌دهد:
• درصد احتمال پیروزی هرکدام (با نوار گرافیکی)
• سابقه رویارویی قبلی این دو بازیکن (برد/باخت/تساوی)

💡 این ابزار صرفاً یک تخمین آماری است، نه پیش‌گویی قطعی.

======================================================================
SECTION: champions_stats — 🏆 آمار، قهرمانان و Elo
======================================================================
--- step 1: جدول رتبه‌بندی Elo ---
📊 *راهنمای جدول Elo*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* منوی اصلی ← 📊 جدول Elo
یا کلمه‌ی `جدول` / `رتبه`

۱۰ بازیکن برتر بر اساس امتیاز Elo، به‌همراه عنوان رسمی هرکدام نمایش داده می‌شود.

💡 روی نام هر بازیکن بزنید تا پنل کامل Elo او (تاریخچه تغییر امتیاز) باز شود.

--- step 2: قهرمان هفته و ماه ---
👑 *راهنمای قهرمانان*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* منوی اصلی ← 👑 قهرمانان
یا کلمه‌ی `قهرمانان`

نمایش می‌دهد:
🌟 قهرمان هفته (بیشترین برد در ۷ روز اخیر)
👑 قهرمان ماه (بیشترین برد در ۳۰ روز اخیر)
⚡ فعال‌ترین بازیکن هفته (بیشترین تعداد بازی)

💡 هر دوشنبه ساعت ۹ صبح، قهرمان هفته به‌صورت خودکار اعلام می‌شود (در صورت تنظیم گروه/کانال اعلانات).

--- step 3: جدول کلی مسابقات (Bracket) ---
📋 *راهنمای جدول مسابقات*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* مدیریت مسابقات ← 📋 جدول مسابقات

یک نمای کلی از وضعیت همه‌ی مسابقات نشان می‌دهد:
• نوار پیشرفت (چند درصد مسابقات نتیجه دارند)
• ۵ نتیجه‌ی آخر
• ۵ مسابقه‌ی در انتظار نتیجه

از همین صفحه می‌توانید مستقیم به جدول Elo یا قهرمانان بروید.

--- step 4: آمار سریع و کلمات میان‌بر ---
📊 *راهنمای آمار سریع*
━━━━━━━━━━━━━━━━━━━━

برای دسترسی سریع بدون منوگردی، این کلمات را بفرستید:

`آمار` — آمار کلی بازیکنان و مسابقات
`نتایج` — آخرین ۱۰ نتیجه
`اخطارها` — لیست بازیکنان دارای اخطار

💡 این کلمات فقط برای مدیر ارشد و مدیران فعال کار می‌کنند.

======================================================================
SECTION: ai_assistant — 🤖 دستیار هوشمند
======================================================================
--- step 1: دستیار هوشمند چیست ---
🤖 *راهنمای دستیار هوشمند*
━━━━━━━━━━━━━━━━━━━━

دستیار هوشمند یک چت‌بات داخلی است که می‌توانید با زبان عادی (نه دکمه) از آن بخواهید کاری برایتان انجام دهد یا سوالی درباره وضعیت سیستم بپرسید.

📌 *باز کردن:*
کلمه‌ی `دستیار` یا `دستیار هوشمند` را بفرستید، یا از منو «🤖 دستیار هوشمند» را بزنید.

📌 *بستن:* کلمه‌ی `بسه` یا `خروج از دستیار`

⚠️ *دسترسی:* فقط مدیر ارشد و مدیرانی که این دسترسی برایشان فعال شده باشد می‌توانند از دستیار استفاده کنند.

--- step 2: چه کارهایی می‌تواند انجام دهد ---
🤖 *راهنمای دستیار — قابلیت‌ها*
━━━━━━━━━━━━━━━━━━━━

می‌توانید با جمله‌ی عادی از دستیار بخواهید، مثلاً:

«محمد رضایی رو توی کلاس ۹۰۱ ثبت کن»
«یه اخطار به آرتین احمدی بده»
«نتیجه‌ی مسابقه رو ثبت کن، برنده سفید»
«آمار سریع رو نشونم بده»
«یه بیانیه بفرست که فردا تعطیله»

دستیار تشخیص می‌دهد کدام تابع را باید صدا بزند و آن را اجرا می‌کند؛ بعد از هر اقدام واقعی، یک «گزارش سیستم» زیر پیام نشان می‌دهد که دقیقاً چه چیزی تغییر کرد.

--- step 3: گفت‌وگوی عادی و تاریخچه ---
🤖 *راهنمای دستیار — گفت‌وگو*
━━━━━━━━━━━━━━━━━━━━

برای سوال‌های تحلیلی یا گزارش‌گیری (نه اجرای عملیات)، دستیار مستقیم با متن جواب می‌دهد؛ نیازی به دستور اجرایی نیست.

📌 *منوی دستیار:*
• 🆕 چت جدید — شروع مکالمه‌ی تازه (تاریخچه‌ی فعلی بسته می‌شود)
• 📜 تاریخچه — مکالمات قبلی خودتان را دوباره باز کنید
• 🚪 خروج — بستن چت و بازگشت به منوی اصلی

🗂️ *مدیر ارشد:* از «🗂️ سوابق AI ادمین‌ها» می‌تواند مکالمات دستیار هوشمند سایر مدیران را هم مرور کند.

--- step 4: محدودیت‌ها و نکات ---
🤖 *راهنمای دستیار — نکات*
━━━━━━━━━━━━━━━━━━━━

💡 دستیار فقط کارهایی را انجام می‌دهد که نقش شما اجازه دارد؛ حتی اگر دستور بدهید، بدون دسترسی لازم اجرا نمی‌شود.

💡 اگر هوش مصنوعی موقتاً خاموش باشد، پیام «💤 هوش مصنوعی فعلا در دسترس نیست» دریافت می‌کنید.

📌 *روشن/خاموش کردن دستیار (فقط مدیر ارشد):*
پنل مدیر ارشد ← تنظیمات ربات

📌 *دادن/گرفتن دسترسی دستیار به یک مدیر خاص:*
مدیریت مدیران ← انتخاب مدیر ← دسترسی‌ها

======================================================================
SECTION: tasks — 📋 وظایف
======================================================================
--- step 1: اعطای وظیفه (مدیر ارشد) ---
📋 *راهنمای وظایف — اعطا*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* پنل مدیر ارشد ← 📋 اعطای وظیفه

مراحل:
1️⃣ مدیر موردنظر را انتخاب کنید
2️⃣ عنوان وظیفه را بنویسید
3️⃣ توضیحات را بنویسید

وظیفه مستقیم برای آن مدیر ارسال می‌شود و او می‌تواند دریافتش را تأیید کند.

--- step 2: پیگیری وظایف (مدیر) ---
📋 *راهنمای وظایف — پیگیری*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* 📌 پیگیری وظایف

روی هر وظیفه بزنید تا جزئیات را ببینید. برای وظایف در انتظار دو گزینه دارید:

✅ انجام شد — وظیفه تمام‌شده ثبت می‌شود و مدیر ارشد مطلع می‌شود
❌ انجام نشد — باید دلیل را بنویسید؛ مدیر ارشد با دکمه‌ی «🔍 پیگیری علت» می‌تواند دلیل را ببیند.

--- step 3: تاریخچه وظایف ---
📜 *راهنمای وظایف — تاریخچه*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* 📜 تاریخچه وظایف
یا کلمه‌ی `وظیفه` / `وظایف`

فیلترها:
✅ انجام‌شده | ⏳ در انتظار | 📋 همه موارد

======================================================================
SECTION: feedback — 💡 انتقادات و پیشنهادات
======================================================================
--- step 1: ارسال بازخورد ---
💡 *راهنمای بازخورد — ارسال*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* 💡 انتقادات و پیشنهادات
یا کلمه‌ی `فیدبک` / `انتقاد`

۴ نوع بازخورد:
📝 انتقاد
💡 پیشنهاد
🏆 تقدیر (پیام قدردانی)
🔧 درخواست قابلیت جدید (عنوان + توضیح جداگانه پرسیده می‌شود)

همه‌ی بازخوردها مستقیم و فوری برای مدیر ارشد ارسال می‌شوند.

--- step 2: مرور بازخوردها (مدیر ارشد) ---
💡 *راهنمای بازخورد — مرور*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* پنل مدیر ارشد ← 📋 انتقادات و پیشنهادات

می‌توانید بر اساس نوع فیلتر کنید (انتقاد/پیشنهاد/تقدیر/قابلیت) یا همه را با هم ببینید.

======================================================================
SECTION: bulk_register — 📋 ثبت‌نام گروهی
======================================================================
--- step 1: چرا و کجا ---
📋 *راهنمای ثبت‌نام گروهی*
━━━━━━━━━━━━━━━━━━━━

برای ثبت تعداد زیادی بازیکن با هم (مثلاً کل یک کلاس یا چند کلاس)، نیازی به ثبت تک‌تک نیست.

📌 *مسیر:*
مدیریت بازیکنان ← 📋 ثبت‌نام گروهی

💡 این کار در چند ثانیه ده‌ها بازیکن را ثبت می‌کند.

--- step 2: فرمت لیست ---
📋 *ثبت‌نام گروهی — فرمت لیست*
━━━━━━━━━━━━━━━━━━━━

هر بازیکن در یک خط، به این شکل:

`نام کامل/کلاس`

مثال:
`محمد پارسا کریمی/901`
`سارا احمدی/702`

✅ فاصله‌های اضافه دور نام یا کلاس مشکلی ایجاد نمی‌کند.
⚠️ شماره کلاس باید با اعداد انگلیسی باشد (901 نه ۹۰۱).

--- step 3: پیش‌نمایش و تأیید ---
📋 *ثبت‌نام گروهی — پیش‌نمایش*
━━━━━━━━━━━━━━━━━━━━

بعد از فرستادن لیست، یک پیش‌نمایش شماره‌دار نشان داده می‌شود:

✅ ردیف‌های سالم — آماده ثبت
⚠️ ردیف‌های مشکل‌دار — با توضیح خطا (فرمت غلط، کلاس ثبت‌نشده و...)

سه دکمه در پایین:
✅ ثبت و تایید — فقط ردیف‌های سالم ثبت می‌شوند
✏️ ویرایش — اصلاح یک ردیف خاص با شماره‌اش
❌ لغو — کل عملیات لغو می‌شود، هیچ‌چیز ثبت نمی‌شود

--- step 4: ویرایش یک ردیف ---
📋 *ثبت‌نام گروهی — ویرایش*
━━━━━━━━━━━━━━━━━━━━

با زدن «✏️ ویرایش»:

1️⃣ شماره‌ی ردیف موردنظر را بفرستید (فقط عدد)
2️⃣ ربات خط فعلی آن ردیف را نشان می‌دهد
3️⃣ خط درست را به همان فرمت `نام/کلاس` بفرستید

پیش‌نمایش دوباره به‌روزرسانی می‌شود و می‌توانید چندین‌بار پشت‌سرهم ویرایش کنید.

======================================================================
SECTION: teams — 👥 مدیریت تیم‌ها
======================================================================
--- step 1: ساخت تیم ---
👥 *راهنمای تیم‌ها — ساخت*
━━━━━━━━━━━━━━━━━━━━

⚠️ *پیش‌نیاز:* حالت تیمی باید در تنظیمات فعال باشد.

📌 *مسیر:*
مدیریت مسابقات ← مسابقات تیمی ← افزودن تیم

مراحل:
1️⃣ نام تیم
2️⃣ شعار (اختیاری)
3️⃣ انتخاب اعضا (حداقل ۳ نفر)
4️⃣ تاریخ ثبت
5️⃣ نام درخواست‌دهنده

پس از ساخت، یک کد یکتا به تیم اختصاص می‌یابد.

--- step 2: مدیریت اعضا ---
👥 *راهنمای تیم‌ها — اعضا*
━━━━━━━━━━━━━━━━━━━━

📌 روی نام تیم ← بازیکنان تیم

عملیات روی هر عضو:
🗑️ حذف از تیم
👑 تنظیم به‌عنوان سرگروه
🌟 ثبت به‌عنوان برتر تیم
⚡ ثبت به‌عنوان نیروی ویژه

💡 *سرگروه:* فقط از اعضای همان تیم انتخاب می‌شود.

--- step 3: آمار تیم ---
👥 *راهنمای تیم‌ها — آمار*
━━━━━━━━━━━━━━━━━━━━

در پنل هر تیم نمایش داده می‌شود:

📊 *آمار کامل:*
• تعداد اعضا و کلاس‌ها
• برد/مساوی/باخت
• بازیکنان ستاره و ویژه
• رتبه بین تیم‌ها
• سطح قدرت (نوار گرافیکی)
• احتمال پیروزی
• اخطارهای تیم

======================================================================
SECTION: automation — 📡 اعلانات و یادآورها
======================================================================
--- step 1: گروه اعلانات و کانال اعلانات ---
📡 *راهنمای اعلانات — گروه و کانال*
━━━━━━━━━━━━━━━━━━━━

این دو کاملاً مستقل از هم هستند:

📡 *گروه اعلانات* — پنل مدیر ارشد ← گروه اعلانات
📢 *کانال اعلانات* — پنل مدیر ارشد ← تنظیم کانال اعلانات

برای اضافه‌کردن ربات به کانال به‌عنوان ادمین:
پنل مدیر ارشد ← «📢 افزودن به کانال» را بزنید

سپس آیدی عددی کانال را (مثلاً `-1001234567890`) در «تنظیم کانال اعلانات» ثبت کنید.

--- step 2: پخش خودکار — چه چیزی کجا برود ---
📡 *راهنمای اعلانات — پخش خودکار*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* پنل مدیر ارشد ← 📡 پخش خودکار

برای هرکدام از این ۴ مورد، ارسال به گروه و کانال جداگانه کنترل می‌شود:

📢 بیانیه‌ها
♟️ نتیجه مسابقات
🏆 قهرمان هفتگی
⏰ یادآورها

مثلاً می‌توانید نتایج را فقط به کانال بفرستید ولی بیانیه‌ها را فقط به گروه.

--- step 3: یادآورهای مدیر ارشد ---
⏰ *راهنمای یادآورها*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* پنل مدیر ارشد ← ⏰ یادآورها

۴ نوع یادآور، هرکدام جداگانه فعال/غیرفعال و زمان‌بندی می‌شود:

♟️ مسابقات بدون نتیجه
📋 وظایف انجام‌نشده
🗄️ اتصال دیتابیس
👤 مدیران غیرفعال (بیش از ۲ روز)

برای هرکدام با دکمه‌ی «⏰ بازه» می‌توانید فاصله‌ی ارسال (۱ تا ۴۸ ساعت) را تنظیم کنید.

ربات هر ساعت یک‌بار چک می‌کند و فقط وقتی زمانش رسیده باشد پیام می‌فرستد.

======================================================================
SECTION: backup — 💾 پشتیبان‌گیری
======================================================================
--- step 1: تهیه بکاپ ---
💾 *راهنمای بکاپ — تهیه*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر (فقط مدیر ارشد):*
پنل مدیر ارشد ← دریافت بکاپ

مراحل:
1️⃣ بازه زمانی را انتخاب کنید:
   📅 امروز | 📆 هفته | 🗓️ ماه | 📚 از ابتدا

2️⃣ فرمت را انتخاب کنید:
   📄 Word | 📊 Excel

فایل بکاپ مستقیم برای مدیر ارشد ارسال می‌شود.

⚡ *بکاپ اضطراری:* دستور /backup_now

🔄 *بکاپ خودکار دوره‌ای:* پنل مدیر ارشد ← دریافت بکاپ ← تنظیمات بکاپ خودکار (می‌توانید فاصله زمانی، فرمت و بازه را تنظیم کنید)

--- step 2: سال تحصیلی جدید ---
💾 *راهنمای بکاپ — سال جدید*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* پنل مدیر ارشد ← 🎓 سال تحصیلی جدید

⚠️ *این عملیات:*
• بکاپ خودکار از تمام داده‌ها تهیه می‌کند
• تمام بازیکنان، مسابقات و تیم‌ها پاک می‌شوند
• سیستم برای سال جدید آماده می‌شود

🔐 *رمز تأیید لازم است.*

💡 بکاپ سال قبل همیشه در دسترس است.

--- step 3: بازگردانی از فایل بکاپ ---
📥 *راهنمای بکاپ — بازگردانی*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* پنل مدیر ارشد ← دریافت بکاپ ← 📥 بازگردانی از فایل

مراحل:
1️⃣ فایل بکاپ (Excel) را ارسال کنید
2️⃣ ربات محتوای فایل را پیش‌نمایش می‌دهد
3️⃣ تأیید کنید تا داده‌ها جایگزین/اضافه شوند، یا لغو کنید

⚠️ *این عملیات حساس است؛ فقط مدیر ارشد به آن دسترسی دارد.*

--- step 4: خزانه مدیر ارشد ---
🏦 *راهنمای خزانه مدیر ارشد*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* پنل مدیر ارشد ← 🏦 خزانه مدیر ارشد

یک نمای متمرکز از:
📂 پرونده‌ی تمام بازیکنان (با دسترسی مستقیم به پنل هرکدام)
🗄️ لیست تمام بکاپ‌های تهیه‌شده (برچسب، فرمت و تاریخ)

💡 مناسب برای مرور سریع کل آرشیو سیستم در یک صفحه.

======================================================================
SECTION: settings — ⚙️ تنظیمات
======================================================================
--- step 1: تنظیمات اصلی ---
⚙️ *راهنمای تنظیمات*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* پنل مدیر ارشد ← تنظیمات ربات

🔧 *تنظیمات قابل تغییر:*
• 🔔 اعلانات (روشن/خاموش)
• 📡 مخابرات
• ❓ راهنما
• ♟️ ثبت مسابقه
• 🚪 ورود ادمین‌ها
• 💤 خاموش کردن ربات
• 🏆 حالت تیمی
• 📝 ثبت‌نام با تیم

--- step 2: وضعیت دیتابیس (دستی) ---
🗄️ *راهنمای تنظیمات — وضعیت دیتابیس*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* پنل مدیر ارشد ← 🗄️ وضعیت دیتابیس

این یک وضعیت کاملاً دستی است که فقط برای نمایش به دیگران استفاده می‌شود و مستقل از اتصال واقعی ربات به دیتابیس است.

🟢 فعال / ⚠️ غیرفعال — با یک دکمه قابل تغییر است.

💡 اگر می‌خواهید از قطعی واقعی دیتابیس باخبر شوید، از «⏰ یادآورها ← 🗄️ اتصال دیتابیس» استفاده کنید.

--- step 3: وضعیت‌های سیستم ---
⚙️ *راهنمای تنظیمات — وضعیت*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* پنل مدیر ارشد ← مدیریت وضعیت
یا کلمه‌ی `امنیت` / `وضعیت`

🚦 *چهار وضعیت:*

🟢 *نرمال:* همه دسترسی‌ها فعال

🟡 *بد:* ادمین‌ها نمی‌توانند حذف/ویرایش کنند

🔴 *خطرناک:* دسترسی همه ادمین‌ها قطع می‌شود

🪽 *APS:* کنترل به سیستم امنیتی واگذار می‌شود

--- step 4: حالت تعمیر و آپدیت ---
🔧 *راهنمای تنظیمات — حالت تعمیر*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* پنل مدیر ارشد ← 🔧 حالت تعمیر

برای زمانی که می‌خواهید ربات را موقتاً برای آپدیت یا نگه‌داری در دسترس ادمین‌ها نگذارید.

🔧 فعال‌سازی تعمیر — همه‌ی مدیران با پیام اطلاع‌رسانی می‌شوند
📝 ثبت دلیل تعمیر — دلیل در همان پیام اطلاع‌رسانی نمایش داده می‌شود
✅ غیرفعال‌سازی تعمیر — بازگشت به حالت عادی، با اطلاع‌رسانی مجدد

💡 فعال‌سازی حالت تعمیر به‌طور خودکار «حالت آپدیت ربات» را هم فعال می‌کند.

--- step 5: تغییر هویت (نام نمایشی) ---
🪪 *راهنمای تنظیمات — تغییر هویت*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* پنل مدیر ارشد ← 🪪 تغییر هویت

🪪 تغییر نام مدیر ارشد — نام نمایشی خودِ مدیر ارشد در سراسر ربات (پیام‌ها، گزارش‌ها و...) عوض می‌شود

👥 تغییر نام مدیران — نام نمایشی سایر مدیران را می‌توانید ویرایش کنید

💡 این فقط نام نمایشی داخل ربات را تغییر می‌دهد؛ اکانت تلگرام دست‌نخورده می‌ماند.

--- step 6: ساعت کاری ---
🕐 *راهنمای تنظیمات — ساعت کاری*
━━━━━━━━━━━━━━━━━━━━

📌 *مسیر:* پنل مدیر ارشد ← 🕐 ساعت کاری

🟢 آغاز ساعت کاری — دسترسی ادمین‌ها باز می‌شود؛ می‌توانید مدت خودکار پایان (به دقیقه) را هم تنظیم کنید

🔴 پایان ساعت کاری — دسترسی بسته می‌شود

⏰ یادآور پایان — قبل از پایان خودکار، یک یادآور به مدیر ارشد ارسال می‌شود (زمانش قابل تنظیم است)

💡 خارج از ساعت کاری، اکثر دکمه‌ها برای مدیران غیرفعال می‌شوند.

--- step 7: بستن خودکار پنل‌های گروهی ---
⏱ *راهنمای تنظیمات — بستن خودکار پنل*
━━━━━━━━━━━━━━━━━━━━

هر پنلی که در یک گروه یا سوپرگروه (نه پیوی) باز می‌شود، اگر صاحبش تا ۳ دقیقه هیچ دکمه‌ای نزند، به‌صورت خودکار بسته می‌شود (دکمه‌ها حذف و متن جایگزین می‌شود).

با هر کلیک روی دکمه، تایمر ۳ دقیقه‌ای از نو شروع می‌شود.

💡 برای باز کردن دوباره، کافیست کلمه‌ی `پنل` را بفرستید.

======================================================================
SECTION: security_aps — 🛡️ امنیت، بلاک و اورژانسی
======================================================================
--- step 1: پنل امنیتی APS ---
🛡️ *راهنمای امنیت — پنل APS*
━━━━━━━━━━━━━━━━━━━━

APS سیستم مدیریت درخواست‌های دسترسی و بلاک کاربران است.

📌 *مسیر:* منو ← 🛡️ پنل امنیتی APS
یا کلمه‌ی `درخواست`

📥 درخواست‌های دسترسی — کاربران جدیدی که منتظر تأیید هستند
🚫 بلاک‌شده‌ها — لیست کاربران بلاک‌شده، با امکان آنبلاک

--- step 2: بررسی صف درخواست‌ها ---
⏳ *راهنمای امنیت — صف انتظار*
━━━━━━━━━━━━━━━━━━━━

روی هر درخواست در صف بزنید تا سه گزینه ببینید:

✅ تأیید — کاربر آزاد می‌شود و می‌تواند از ربات استفاده کند
🔓 خروج از صف بدون تأیید — درخواست نادیده گرفته می‌شود
🚫 بلاک دائم — کاربر برای همیشه مسدود می‌شود

💡 تا وقتی کاربری در صف است، پیام می‌بیند که درخواستش در حال بررسی است.

--- step 3: بلاک و آنبلاک مستقیم ---
🚫 *راهنمای امنیت — بلاک*
━━━━━━━━━━━━━━━━━━━━

کاربر بلاک‌شده هیچ پیام یا دکمه‌ای نمی‌تواند به ربات بفرستد و پیام «دسترسی شما توسط واحد امنیتی APS محدود شده» می‌بیند.

📌 *آنبلاک:* پنل امنیتی APS ← 🚫 بلاک‌شده‌ها ← انتخاب کاربر ← 🔓 آنبلاک

--- step 4: دستورات اضطراری (فقط مدیر ارشد) ---
🚨 *راهنمای امنیت — دستورات اضطراری*
━━━━━━━━━━━━━━━━━━━━

این‌ها دستورات اسلش (/) هستند، فقط برای مدیر ارشد:

`/panic` — بلافاصله وضعیت سیستم را «خطرناک» می‌کند و دسترسی همه‌ی مدیران قطع می‌شود؛ همه با پیام هشدار مطلع می‌شوند

`/unpanic` — بازگشت به وضعیت نرمال

`/freeze_all` — کنترل کامل را به وضعیت APS می‌سپارد

`/terminal` — نمایش سریع وضعیت سیستم (تعداد مدیران، بازیکنان، مسابقات، حجم دیتابیس) به‌صورت خام

`/backup_now` — بکاپ اضطراری فوری از کل داده‌ها

`/override_strike [آیدی مدیر] [عدد]` — تغییر دستی تعداد اخطارهای یک مدیر (برای اصلاح خطا)

======================================================================
SECTION: troubleshoot — 🆘 عیب‌یابی
======================================================================
--- step 1: دکمه‌ای کار نمی‌کند ---
🆘 *عیب‌یابی — دکمه کار نمی‌کند*
━━━━━━━━━━━━━━━━━━━━

دلایل احتمالی:

1️⃣ *وضعیت سیستم:*
   مدیر ارشد سیستم را در وضعیت محدود قرار داده
   → با مدیر ارشد تماس بگیرید

2️⃣ *ساعت کاری:*
   خارج از ساعت کاری هستید
   → منتظر باز شدن ساعت کاری باشید

3️⃣ *دسترسی:*
   مدیر ارشد این دسترسی را برای شما غیرفعال کرده
   → از مدیر ارشد درخواست دسترسی کنید

--- step 2: مسابقه ثبت نمی‌شود ---
🆘 *عیب‌یابی — ثبت مسابقه*
━━━━━━━━━━━━━━━━━━━━

```
❌ ERROR_CODE: 001
```
کمتر از ۲ بازیکن ادامه‌دهنده وجود دارد.

راه‌حل‌ها:
✅ بازیکنان بیشتری ثبت کنید
✅ بازیکنان حذف‌شده را احیا کنید
✅ بازیکنان تعلیق‌شده را بازگردانید

```
❌ ERROR_CODE: 002
```
هیچ کلاسی ثبت نشده.

راه‌حل: مدیریت بازیکنان ← مدیریت کلاس‌ها ← ثبت کلاس جدید

--- step 3: بازیکن پیدا نمی‌شود ---
🆘 *عیب‌یابی — بازیکن پیدا نمی‌شود*
━━━━━━━━━━━━━━━━━━━━

احتمالات:

1️⃣ *بازیکن در لیست ادامه‌دهندگان نیست:*
   ممکن است حذف، تعلیق یا اخراج باشد
   → مدیریت بازیکنان ← حذف‌شدگان

2️⃣ *نام اشتباه وارد شده:*
   → مدیریت بازیکنان ← جستجو بازیکن

3️⃣ *اخطار بیش از حد:*
   بازیکن با ۳ اخطار از لیست ادامه‌دهندگان خارج می‌شود

--- step 4: اطلاعات اشتباه ثبت شده ---
🆘 *عیب‌یابی — اطلاعات اشتباه*
━━━━━━━━━━━━━━━━━━━━

ویرایش مسابقه:
تاریخچه مسابقات ← روی مسابقه بزنید ← ✏️ ویرایش

ویرایش نام بازیکن:
روی بازیکن بزنید ← ✏️ ویرایش نام

ویرایش کلاس بازیکن:
روی بازیکن بزنید ← 🏫 ویرایش کلاس

⚠️ *حذف مسابقه* فقط توسط کسانی که دسترسی دارند ممکن است.


---

## ۶) چطور دستیار باید از این سند استفاده کند

- اگر کاربر بپرسد «فلان کار رو از کجا انجام بدم؟» یا «دکمه‌ی X کجاست؟»، دقیقاً بر اساس بخش‌های
  ۲ و ۵ همین سند، مسیرِ کاملِ دکمه‌ها را قدم‌به‌قدم بگو (نه حدسی/تقریبی).
- اگر کاربر خواستِ اجراییِ مشخصی داشت که در جدولِ بخش ۴ یک تابعِ متناظر دارد و نقشش مجاز است،
  به‌جای توضیح مسیر دکمه‌ها، مستقیم همان تابع را صدا بزن (سریع‌تر و دقیق‌تر از هدایت کاربر به منو).
- اگر کاری هم از طریق دکمه/کلمه در دسترس است هم دستیار می‌تواند خودش انجامش دهد، برای اجرای فوری
  از ابزار استفاده کن؛ فقط وقتی توضیح مسیر منو بده که کاربر صراحتاً پرسیده «از کجا» یا دستیار
  ابزار متناظر را در اختیار ندارد (نقشش اجازه نمی‌دهد یا دسته‌ابزار خاموش است) — در این حالت
  رک بگو این کار از طریق منو/دکمه‌ی فلان انجام می‌شود.
- اگر عملیاتِ خواسته‌شده برای نقشِ کاربر مجاز نیست (نه در بخش ۲، نه در بخش ۴)، مؤدبانه توضیح بده
  که این کار خارج از دسترسیِ نقش اوست و کدام نقش (یا فقط مدیر ارشد) می‌تواند انجامش دهد.
